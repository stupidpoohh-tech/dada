# DADA TOWN 기획안

> 앱·문서·영상·공간을 아우르는 작업물과 "내 세계"를 담는 픽셀 마을. 링크드인 역할 겸용.

작성일: 2026-08-14 · **v5 (수록 항목 정리 반영)**

## 확정 사항

| 항목 | 결정 |
|---|---|
| 이름 | **DADA TOWN** |
| 지도 | 가로 배경 **1장**으로 PC·모바일 모두 커버 ([ASSETS.md](ASSETS.md)) |
| PDF 문서 | **원본 노출·다운로드 금지.** 웹 콘텐츠로 새로 기획·구성 |
| 신원 | 실명 + 약력 + 연락처 전부 공개 (링크드인 대체) |
| 수록 방식 | **사용자가 넣기로 정한 것만 추가.** 후보를 미리 늘어놓지 않는다 |

### v5에서 바뀐 것

초기 기획은 후보 30개를 먼저 늘어놓고 채워 넣는 방식이었다. 그중 **실제로 넣기로 한 것만 남기고 16개를 폐기**했다 (목록은 [ITEMS.md](ITEMS.md)). 그래서 이 문서에서도 폐기된 것에 딸린 설계가 함께 빠졌다 — 영상 게재 계획 4종, 문서 템플릿 B(갤러리형)·C(폼형), 게임 가이드 카탈로그 설계.

앞으로 항목은 사용자가 URL이나 재료를 줄 때 하나씩 추가한다. **채워야 할 빈칸 목록을 이 문서가 들고 있지 않는다** — 빈칸이 많아 보이는 계획서는 진행 상황을 왜곡한다.

---

## 1. 컨셉

픽셀아트 탑뷰 마을. 자동차가 도로를 돌고 새가 날아다닌다. **구역(건물)을 누르면 그 안의 작업물이 나온다.**

"웹서비스 모음"이 아니라 **"작업물과 세계의 총체"**다. 마을은 링크 모음이 아니라 **내가 무엇을 만들고 어디에 서 있는 사람인지 보여주는 자기소개**다. 앱만 있는 게 아니라 전시, 공간 운영, 사업 아카이브가 함께 놓이는 것이 이 포트폴리오의 차별점.

## 2. 구역 구성 (9구역)

| 구역 | 건물 형태 | 수록 항목 | 개수 |
|---|---|---|---|
| 🏫 **학교** | 학교 (운동장) | 고교 문법 · 보카 · 리스닝 · 여행 중국어 | 4 |
| 🖼 **미술관** | 미술관 | 아트 포트폴리오 (아트북 뷰어) | 1 |
| 🏢 **회사** | 오피스 빌딩 | 업무공유관리툴 demo | 1 |
| 🏛 **성균관** | 한옥 (기와·마당) | 에어비앤비 체험 | 1 |
| 🏦 **은행** | 은행 | 잔고 캘린더 · 조기상환 시뮬레이션 | 2 |
| 🌳 **사람들** | 공원 — 사람들이 모여 있는 | OLRW 전보 메신저 · 매일 AI | 2 |
| ☕ **카페** | 카페 | ANIH 동선 플래너 | 1 |
| 🔺 **세모집** | 삼각지붕 집 | Dada's House of ▼riangles (카탈로그) | 1 |
| 🙋 **나** | **세모집 앞에 서서 손 흔드는 캐릭터** | 약력 (구역 자체가 프로필) | — |

현재 수록 13개. **공항 없음.**

**"사람들"이 공원인 이유**: 건물이 아니라 사람이 모여 있는 장면 자체가 구역의 정체성이다. 마을에서 유일하게 사람이 그려지는 곳이라 시선이 자연히 머문다.

**"나"가 건물이 아니라 캐릭터인 이유**: 이 마을을 만든 사람은 건물 안에 들어가 있는 게 아니다. 세모집(내 집) 앞에 서서 손을 흔들고 있고, 클릭하면 약력이 열린다.

실용적으로도 이게 낫다 — **손 흔드는 캐릭터는 "여기 있어요, 눌러보세요"가 그림 자체에 들어 있어서** 첫 방문자에게 안내 문구 없이도 클릭을 유도한다.

### 약력은 문서가 아니라 구역이다

초기 기획의 「약력도」는 별도 문서 페이지(`/docs/profile`)였지만, **"나" 구역을 누르면 바로 열리는 프로필**로 구현하는 편이 나았다. 문서 한 장을 더 만들 필요가 없고, 마을 안에서 흐름이 끊기지 않는다.
내용은 `services.json`의 `profile` — 이름 · 한 줄 정체성 · 경력 · 학력 · 이메일. 여기만 고치면 "나" 패널과 푸터가 함께 바뀐다.

### 외부 채널의 자리

공항 구역이 없으므로 이메일·채널 링크는 두 군데에 둔다.

1. **"나" 구역 프로필** — 링크드인 역할의 본체
2. **사이트 푸터** — 어느 화면에서든 닿을 수 있게

## 3. 콘텐츠 타입

성격이 다른 것들이 한 마을에 섞이므로 타입을 명시적으로 구분한다. 카드에 타입 배지를 달고, 목록에서 필터로 쓴다.

| 타입 | 배지 | 동작 | 현재 |
|---|---|---|---|
| **앱** | 🖥 | 새 탭으로 서비스 열기 | 10 |
| **문서** | 📄 | 사이트 안에서 열기 | 2 (아트북 · 세모집 카탈로그) |
| **외부** | 🔗 | 외부 플랫폼으로 이동 | 1 (에어비앤비) |
| **영상** | 🎬 | 카드 안에서 재생 | 0 |

영상 타입은 코드에 남겨둔다 — 나중에 영상을 넣기로 하면 `"type": "video"`, `"youtubeId"`, `"duration"`만 채우면 된다. 지금 게재할 영상은 없다.

## 4. Dada's House of ▼riangles — 세모집 카탈로그

**원칙: PDF 원본을 그대로 웹에 올리지 않는다. 다운로드 링크도 만들지 않는다.**
다만 **문서의 성격에 맞는 형태**를 찾는 것이지, 무조건 새로 짓는 것이 아니다. 이 문서는 원본이 이미 옳은 형태였다.

### 무엇을 담은 문서인가

**주어는 사람이 아니라 집이다.** "내 취미"가 아니라 **"이 집에서 할 수 있는 것"**의 목록이다.
표지가 `나의 세모집에 초대합니다`라고 말하듯 초대장의 어조를 갖되, 손님이 고르는 신청서는 아니다.
문 열어놓고 "구경하고 가세요"에 가깝다.

표지 1장 + 본문 5장, 항목은 크게 29가지 · 세부까지 53가지.

| 장 | 표제 | 부제 |
|---|---|---|
| 표지 | Dada's House of ▼riangles | 나의 세모집에 초대합니다 |
| 1 | Signature Experience | 아주 특별한 순간 |
| 2 | Let the Game Begin | 당신이 알던 게임이 아닐 게임 |
| 3 | Home and,made | 비트코인의 시대에서 무언가를 손으로 만든다는 것 |
| 4 | Art, Brush, Color | 가끔 나에게 다원이는 그림을 시켜 |
| 5 | Dream to Real | 항상 상상한 세상 |

### 왜 카탈로그(넘겨보는 형태)인가

**비행기 좌석 앞에 꽂힌 카탈로그처럼 한 장씩 넘겨본다.** 사려고 보는 게 아니라 심심해서 넘기는 물건이고, 넘긴 사람은 뭘 샀는지가 아니라 **"별게 다 있더라"**를 기억한다. 이 문서가 남기려는 인상이 정확히 그것이다.

원본 5장이 이미 그 형식이다 — 한 장 = 한 주제, 큰 이미지 하나 + 항목 목록 + 오른쪽 정렬된 소요시간. 형식을 새로 씌우는 게 아니라 **원래 그거였던 것을 제자리에 놓는 일**이다.

§4-9에서 46쪽 게임 가이드에 플립북이 부적합하다고 본 것과 모순되지 않는다. 그건 **찾는 것**이었고 이건 **훑는 것**이다. 6장이라 짧고, 넘기는 행위는 스크롤과 달리 손에 남는다.

### 왜 사진으로 짓지 않았나

집 실물은 최대한 노출하지 않는다. 방 배치·현관 위치·살림이 읽히는 것은 이 문서의 목적이 아니다.
표지의 **거실 입면도**는 예외다 — 도면은 공간을 보여주면서 "여기가 어디인지"를 흘리지 않는다.
(픽셀 아이소메트릭 내부 도면도 있으나 방 구조가 그대로 읽혀 쓰지 않는다.)

### 구현

**원본을 편집해 이미지로 뽑고, 아트북과 같은 뷰어로 넘긴다.** 원본 편집 파일이 있으므로 항목 수정·이미지 교체가 원본 쪽에서 가능하다.

- 경로 — `assets/portfolio/house/p01~p06.jpg`(높이 2000px) + 썸네일 `t01~t06.jpg`
- 뷰어 — `open: "book"`으로 아트북과 공유. 책마다 `dir`·`pages`·`hash`·`ratio`·`flat`만 다르다
- 아트북은 **책**(종이 두께·책등 그늘), 카탈로그는 **브로슈어**(`flat`)로 넘김 인상을 구분한다.
  같은 인터랙션이 두 구역에 그대로 있으면 마을이 단조로워 보인다
- 세로 페이지는 가로형 지도 안에서 높이가 모자라므로 화면 전체를 쓴다
- 세모집 구역은 항목이 하나뿐이므로 미술관처럼 `direct`로 바로 연다
- 원본 PDF·편집 파일은 레포에 두지 않는다

남는 약점 둘 — 모바일에서 글씨가 작다(세로 A4 비율이라 폰과 방향이 같아 견딜 만하다), 검색엔진·스크린리더가 못 읽는다(`/list` 페이지로 부분 보완). 나중에 HTML로 다시 짜면 둘 다 해결되지만, 원본 편집 디자인을 90%밖에 못 가져온다.

### 원본에서 손볼 것

`* 맥심 VR`은 2장에서 뺐다 — 그 자리에 `스포츠`(탁구 · 복싱 · 농구)가 들어갔다.

| 항목 | 처리 |
|---|---|
| `옷구경 & 인형놀이에서 인형이 되기 (손님이)` (1장) | 손님을 전제한 문구라 다시 쓴다 |
| `퍼스널컬러전문가자격 소유자의 진단` (1장) | 집이 아니라 사람의 자격이라 주어가 어긋난다 |
| VR 헤드셋 · Xbox 컨트롤러 (2장) | 가져온 제품 이미지면 실물을 흰 배경에 놓고 직접 찍어 교체 |
| 표지 배경의 「네모의 꿈」 가사 | 웹 공개는 인쇄물 개인 소장과 다르다. 남길지 판단 필요 |
| 스팀 게임 줄임말 (`디비휴` · `뱀서` · `성세천하`) | 처음 보는 사람은 못 알아본다 |

### 영상을 넣을 때 지킬 것

1. **썸네일 지연 로딩 (lite-embed 패턴)** — 처음엔 썸네일 + 재생 버튼만 두고, 클릭할 때 iframe을 삽입한다. 유튜브 iframe은 하나당 수백 KB를 끌어오므로 이걸 안 하면 페이지가 눈에 띄게 느려진다.
2. **`youtube-nocookie.com` 도메인 사용** — 재생 전까지 추적 쿠키를 심지 않는다.

일부공개는 완전한 비공개가 아니다 — 링크가 유출되면 누구나 볼 수 있다.

## 5. 전체 목록

항목이 13개 안팎이면 마을만으로도 탐색이 되지만, 훑어보고 싶은 사람을 위해 목록을 한 번의 조작으로 연다.

**구현 완료** — 목록 모달: 구역별 그룹 헤더 + 카드(아이콘 · 이름 · 한 줄 설명 · 타입 배지), 즉시 검색, 타입 필터 칩(개수 표시, 0개인 타입은 칩 자체가 안 생김), 바깥 클릭·ESC·닫기 버튼, 포커스 트랩.

**남은 것 — `/list` 정적 페이지.** 모달 안의 내용은 검색엔진과 스크린리더가 읽지 못한다. 동일한 `services.json`으로 렌더링한 정적 전체 목록을 두고, sitemap에 포함시키고, 모달 하단에서 링크한다. 마을은 경험, `/list`는 정보로 역할이 갈린다.

## 6. PC / 모바일 대응

**배경 이미지는 가로 1장으로 PC·모바일을 모두 커버한다.** 세로 시안은 만들지 않는다.

지도가 3열 블록이라 모바일 폭(390px)에 맞춰 축소해도 블록 하나가 약 130px, 가장 작은 한옥도 약 60px로 터치 기준(44px)을 넉넉히 넘는다. 320px 화면에서도 50px 수준이라 문제없다.

| | PC | 모바일 |
|---|---|---|
| 지도 | 화면 폭 기준, 최대 1440px | 폭에 맞춤, 세로 가운데 정렬 |
| 구역 열기 | 누른 건물 **바로 옆에** 팝오버 (위치는 JS가 계산) | 지도 **아래에 패널**이 흐름대로 붙음 |
| 목록 | 상단 버튼 → 중앙 모달 | 상단 버튼 → 모달, 추천 픽은 하단 시트 |
| 터치 영역 | — | 최소 44×44px 보장 |

초기 기획의 "모바일 하단 전체 카드 리스트"는 넣지 않았다. 항목이 13개 안팎이라 지도를 눌러 패널을 여는 것으로 충분하고, 상시 리스트는 지도를 화면 밖으로 밀어낸다.

## 7. 기술 설계

- **데이터 단일 소스** — `services.json` 하나로 마을 팝오버 · 목록 모달 · 프로필 · 푸터를 전부 렌더링
- **지도** — 픽셀 배경(`image-rendering: pixelated`) + 구역 위치에 `<button>` 핫스팟. 고정 종횡비 컨테이너 안에서 % 좌표를 쓰므로 화면 크기가 바뀌어도 좌표가 어긋나지 않는다
- **애니메이션** — 자동차는 도로 구간(`CAR_ROUTES`)을 wrap/pingpong으로 순환, 새·구름은 느린 수평 이동, 건물·사람은 `folk-idle` 2.4초 한 박자. **사용자 결정으로 OS '동작 줄이기'와 무관하게 상시 재생.** 구현 주의사항은 [README](README.md)에 정리
- **스택** — 프레임워크 없이 HTML + CSS + vanilla JS. 빌드 과정 없음
- **배포** — Cloudflare Pages (이 레포 연결, 루트를 그대로 게시)
- **원본 PDF** — 레포에 두지 않는다

### services.json 스키마

```json
{
  "id": "balance-calendar",
  "name": "잔고 캘린더",
  "district": "bank",
  "group": null,
  "type": "app",
  "url": "https://dada-calendar.pages.dev/",
  "description": "다음 급여까지 쓸 수 있는 돈과 버틸 날짜를 달력으로 봅니다.",
  "icon": "📅",
  "status": "live",
  "featured": true
}
```

- `district` — `museum · school · seonggyungwan · park · company · cafe · bank · house · me`
- `type` — `app`(새 탭) · `doc`(내부) · `video` · `external`
- `group` — 한 구역 안에서 소제목으로 묶을 때
- `status` — `live` · `beta` · `demo`
- `featured` — 상단 "추천 픽"에 노출
- `needsReview` — 설명 문구가 추정본이라 검수가 필요한 항목 표시

## 8. 남은 일

| 일 | 상태 |
|---|---|
| 마을 지도 · 구역 핫스팟 · "나" 캐릭터 · 팝오버 | ✅ |
| 목록 모달 (검색 · 타입 필터) · 추천 픽 | ✅ |
| 앰비언트 애니메이션 (자동차 · 새 · 구름 · 사람 · 건물) | ✅ |
| 아트북 뷰어 | ✅ |
| 뷰어를 여러 권 지원하도록 일반화 | ✅ |
| **세모집 카탈로그** | ✅ 올림 (2장 교체 완료, 나머지 문구는 §4 표) |
| **`/list` 정적 페이지** | 미착수 |
| `services.json` 설명 문구 검수 (`needsReview` 12건) | 미착수 |
| 커스텀 도메인 (`dada.town`) | 선택 |
| 🌙 밤 마을 | 선택 |

## 9. 이름 · 도메인

- 이름: **DADA TOWN** (다다 마을)
- 현재 주소: `dada-portfolio.stupidpoohh.workers.dev`
- 커스텀 도메인(선택): `dada.town` — `*.workers.dev`는 링크 검사기에서 걸러지는 경우가 있어 공유용으로는 커스텀 도메인이 안전하다
- 히어로 카피: "만든 것들이 사는 마을, DADA TOWN."
