# DADA TOWN 기획안

> 앱·문서·영상·공간을 아우르는 작업물과 "내 세계"를 담는 픽셀 마을. 링크드인 역할 겸용.

작성일: 2026-08-14 · **v5 (수록 항목 정리 반영)**

## 확정 사항

| 항목 | 결정 |
|---|---|
| 이름 | **DADA TOWN** |
| 지도 | 가로 배경 **1장**으로 PC·모바일 모두 커버 ([ASSETS.md](ASSETS.md)) |
| PDF 문서 | **원본 노출·다운로드 금지.** 웹 콘텐츠로 새로 기획·구성 |
| 신원 | 실명 + 약력 + 연락처 전부 공개 (링크드인 대체) |
| 수록 방식 | **사용자가 넣기로 정한 것만 추가.** 후보를 미리 늘어놓지 않는다 |

### v5에서 바뀐 것

초기 기획은 후보 30개를 먼저 늘어놓고 채워 넣는 방식이었다. 그중 **실제로 넣기로 한 것만 남기고 16개를 폐기**했다 (목록은 [ITEMS.md](ITEMS.md)). 그래서 이 문서에서도 폐기된 것에 딸린 설계가 함께 빠졌다 — 영상 게재 계획 4종, 문서 템플릿 B(갤러리형)·C(폼형), 게임 가이드 카탈로그 설계.

앞으로 항목은 사용자가 URL이나 재료를 줄 때 하나씩 추가한다. **채워야 할 빈칸 목록을 이 문서가 들고 있지 않는다** — 빈칸이 많아 보이는 계획서는 진행 상황을 왜곡한다.

다만 폐기가 지나쳤던 지점이 하나 있다. §1은 "전시 · 공간 운영 · **사업 아카이브**가 함께 놓이는 것이 차별점"이라고 선언하는데 그 셋을 전부 폐기해 선언과 실물이 벌어졌다. **Playgrown 케이스 스터디를 되살리기로 했다** (2026-08-15). 설계는 §5, 재료 현황은 [ITEMS.md](ITEMS.md).

---

## 1. 컨셉

픽셀아트 탑뷰 마을. 자동차가 도로를 돌고 새가 날아다닌다. **구역(건물)을 누르면 그 안의 작업물이 나온다.**

"웹서비스 모음"이 아니라 **"작업물과 세계의 총체"**다. 마을은 링크 모음이 아니라 **내가 무엇을 만들고 어디에 서 있는 사람인지 보여주는 자기소개**다. 앱만 있는 게 아니라 전시, 공간 운영, 사업 아카이브가 함께 놓이는 것이 이 포트폴리오의 차별점.

## 2. 구역 구성 (9구역)

| 구역 | 건물 형태 | 수록 항목 | 개수 |
|---|---|---|---|
| 🏫 **학교** | 학교 (운동장) | 고등영어문법 · 받아쓰기 리스닝 · 상하이여행중국어 · 매일 AI 한장 | 4 |
| 🖼 **미술관** | 미술관 | Not an Artist, a Strategist (아트북 뷰어) | 1 |
| 🏢 **회사** | 오피스 빌딩 | 사업팀 운영보드 (demo) | 1 |
| 🏛 **성균관** | 한옥 (기와·마당) | 성균관 체험 호스팅 | 1 |
| 🏦 **은행** | 은행 | 잔고캘린더 + 상환 시뮬레이션 | 1 |
| 🌳 **사람들** | 공원 — 사람들이 모여 있는 | Our love, rightly written | 1 |
| ☕ **카페** | 카페 · **앞에 까마귀 Croww** | 아니그래서 (PlayGrown은 다시 짓는 중) | 1 |
| 🔺 **세모집** | 삼각지붕 집 · 마당에 우편함 · **지붕에 확성기** | DADA 홈 카탈로그 · 게임 안내서 | 2 |
| 🙋 **나** | **세모집 앞에 서서 손 흔드는 캐릭터** | 약력 (구역 자체가 프로필) · 클리어 위크 | 1 |

현재 수록 13개. **공항 없음.** 마을 위를 도는 **쪽지**가 「매일 쓰는 것들」을 묶어 연다.

**"사람들"이 공원인 이유**: 건물이 아니라 사람이 모여 있는 장면 자체가 구역의 정체성이다. 마을에서 유일하게 사람이 그려지는 곳이라 시선이 자연히 머문다.

**"나"가 건물이 아니라 캐릭터인 이유**: 이 마을을 만든 사람은 건물 안에 들어가 있는 게 아니다. 세모집(내 집) 앞에 서서 손을 흔들고 있고, 클릭하면 약력이 열린다.

실용적으로도 이게 낫다 — **손 흔드는 캐릭터는 "여기 있어요, 눌러보세요"가 그림 자체에 들어 있어서** 첫 방문자에게 안내 문구 없이도 클릭을 유도한다.

### 약력은 문서가 아니라 구역이다

초기 기획의 「약력도」는 별도 문서 페이지(`/docs/profile`)였지만, **"나" 구역을 누르면 바로 열리는 프로필**로 구현하는 편이 나았다. 문서 한 장을 더 만들 필요가 없고, 마을 안에서 흐름이 끊기지 않는다.
내용은 `services.json`의 `profile` — 이름 · 한 줄 정체성 · 경력 · 학력 · 이메일. 여기만 고치면 "나" 패널과 푸터가 함께 바뀐다.

### 외부 채널의 자리

공항 구역이 없으므로 이메일·채널 링크는 두 군데에 둔다.

1. **"나" 구역 프로필** — 링크드인 역할의 본체
2. **사이트 푸터** — 어느 화면에서든 닿을 수 있게

## 3. 콘텐츠 타입

성격이 다른 것들이 한 마을에 섞이므로 타입을 명시적으로 구분한다. 카드에 타입 배지를 달고, 목록에서 필터로 쓴다.

| 타입 | 배지 | 동작 | 현재 |
|---|---|---|---|
| **앱** | 🖥 | 새 탭으로 서비스 열기 | 9 |
| **문서** | 📄 | 사이트 안에서 열기 | 3 (아트북 · DADA 홈 카탈로그 · 게임 안내서) |
| **외부** | 🔗 | 외부 플랫폼으로 이동 | 1 (에어비앤비) |
| **영상** | 🎬 | 카드 안에서 재생 | 0 |

영상 타입은 코드에 남겨둔다 — 나중에 영상을 넣기로 하면 `"type": "video"`, `"youtubeId"`, `"duration"`만 채우면 된다. 지금 게재할 영상은 없다.

## 4. DADA 홈 카탈로그 (Dada's House of ▼riangles)

**원칙: PDF 원본을 그대로 웹에 올리지 않는다. 다운로드 링크도 만들지 않는다.**
다만 **문서의 성격에 맞는 형태**를 찾는 것이지, 무조건 새로 짓는 것이 아니다. 이 문서는 원본이 이미 옳은 형태였다.

### 무엇을 담은 문서인가

**주어는 사람이 아니라 집이다.** "내 취미"가 아니라 **"이 집에서 할 수 있는 것"**의 목록이다.
표지가 `나의 세모집에 초대합니다`라고 말하듯 초대장의 어조를 갖되, 손님이 고르는 신청서는 아니다.
문 열어놓고 "구경하고 가세요"에 가깝다.

표지 1장 + 본문 5장, 항목은 크게 29가지 · 세부까지 53가지.

| 장 | 표제 | 부제 |
|---|---|---|
| 표지 | Dada's House of ▼riangles | 나의 세모집에 초대합니다 |
| 1 | Signature Experience | 아주 특별한 순간 |
| 2 | Let the Game Begin | 당신이 알던 게임이 아닐 게임 |
| 3 | Home and,made | 비트코인의 시대에서 무언가를 손으로 만든다는 것 |
| 4 | Art, Brush, Color | 가끔 나에게 다원이는 그림을 시켜 |
| 5 | Dream to Real | 항상 상상한 세상 |

### 왜 카탈로그(넘겨보는 형태)인가

**비행기 좌석 앞에 꽂힌 카탈로그처럼 한 장씩 넘겨본다.** 사려고 보는 게 아니라 심심해서 넘기는 물건이고, 넘긴 사람은 뭘 샀는지가 아니라 **"별게 다 있더라"**를 기억한다. 이 문서가 남기려는 인상이 정확히 그것이다.

원본 5장이 이미 그 형식이다 — 한 장 = 한 주제, 큰 이미지 하나 + 항목 목록 + 오른쪽 정렬된 소요시간. 형식을 새로 씌우는 게 아니라 **원래 그거였던 것을 제자리에 놓는 일**이다.

§4-9에서 46쪽 게임 가이드에 플립북이 부적합하다고 본 것과 모순되지 않는다. 그건 **찾는 것**이었고 이건 **훑는 것**이다. 6장이라 짧고, 넘기는 행위는 스크롤과 달리 손에 남는다.

### 왜 사진으로 짓지 않았나

집 실물은 최대한 노출하지 않는다. 방 배치·현관 위치·살림이 읽히는 것은 이 문서의 목적이 아니다.
표지의 **거실 입면도**는 예외다 — 도면은 공간을 보여주면서 "여기가 어디인지"를 흘리지 않는다.
(픽셀 아이소메트릭 내부 도면도 있으나 방 구조가 그대로 읽혀 쓰지 않는다.)

### 구현

**원본을 편집해 이미지로 뽑고, 아트북과 같은 뷰어로 넘긴다.** 원본 편집 파일이 있으므로 항목 수정·이미지 교체가 원본 쪽에서 가능하다.

- 경로 — `assets/portfolio/house/p01~p06.jpg`(높이 2000px) + 썸네일 `t01~t06.jpg`
- 뷰어 — `open: "book"`으로 아트북과 공유. 책마다 `dir`·`pages`·`hash`·`ratio`·`flat`만 다르다
- 아트북은 **책**(종이 두께·책등 그늘), 카탈로그는 **브로슈어**(`flat`)로 넘김 인상을 구분한다.
  같은 인터랙션이 두 구역에 그대로 있으면 마을이 단조로워 보인다
- 세로 페이지는 가로형 지도 안에서 높이가 모자라므로 화면 전체를 쓴다
- 세모집 구역은 미술관처럼 `direct`로 소개서를 바로 연다.
  게임 안내서가 들어와 항목이 둘이 됐지만 팝오버로 묶지 않았다 —
  **물건마다 문이 하나씩이다. 건물은 소개서, 마당의 우편함은 게임 안내서** (§6)
- 원본 PDF·편집 파일은 레포에 두지 않는다

남는 약점 둘 — 모바일에서 글씨가 작다(세로 A4 비율이라 폰과 방향이 같아 견딜 만하다), 검색엔진·스크린리더가 못 읽는다(`/list` 페이지로 부분 보완). 나중에 HTML로 다시 짜면 둘 다 해결되지만, 원본 편집 디자인을 90%밖에 못 가져온다.

### 원본에서 손볼 것

**남은 할 일은 없다.** 한때 「문장만 고치면 끝나는 것」 셋을 따로 세워 뒀는데,
문장이 전부 지면 이미지 안에 있어 결국 원본을 다시 뽑아야 하는 일이었다 —
싼 일이 아니었으므로 판단할 일 쪽으로 옮겨 닫았다 (2026-08-20).

**정해진 것**

| 항목 | 결정 |
|---|---|
| 1장 문장 3건 — 옷구경 문구 · 퍼스널컬러 주어 · 스팀 줄임말 | **그대로 둔다** (2026-08-20). 셋 다 1장 이미지 안에 인쇄돼 있어 고치려면 원본을 다시 뽑아야 하는데, 얻는 것이 그 값에 못 미친다. **다시 꺼내지 않는다** |
| 표지 배경의 「네모의 꿈」 가사 | **그대로 둔다** (2026-08-19). 표지 배경 장식이고 개인 포트폴리오다. 나중에 이 문서를 상업적으로 쓰게 되면 그때 다시 본다 |
| VR 헤드셋 · Xbox 컨트롤러 (2장) | **급하지 않다. 하고 싶을 때 바꾼다.** 아래 |

`* 맥심 VR`은 2장에서 뺐다 — 그 자리에 `스포츠`(탁구 · 복싱 · 농구)가 들어갔다.

### 제품 이미지를 직접 찍는 문제

처음엔 저작권을 이유로 「직접 찍어 교체」라고 적었는데 **그 근거는 과했다.**
제조사 대표 이미지를 개인 비상업 안내서에서 제품을 가리키는 데 쓰는 것은
흔한 관행이고, 이걸 문제 삼으면 휴대폰으로 찍은 사진도 못 쓸 이유가 생긴다.

살아남는 이유는 다른 것이다 — **주어**다. 이 문서의 주어는 사람이 아니라 집인데
(§4 첫머리), 제조사 렌더는 「이 제품이 세상에 있다」까지만 말하고 **「그게 이 집에 있다」**는
말하지 않는다. 실물 사진은 그걸 말한다. 그래서 바꾸면 문서가 조금 더 자기 주장에 맞는다.

다만 **조금 더**일 뿐이라 우선순위를 낮춘다. 6면 53항목짜리 문서에서 항목마다
전용 사진이 필요하지도 않다. 언젠가 실물을 찍게 되면 그때 갈아 끼운다.

### 지면 배경의 흐린 표식은 의도한 것이다

2 · 3면 배경을 대비를 올려 보면 `©` 모양 고리와 회전된 흐린 글자가 규칙적으로
반복된다. 스톡 소재의 워터마크가 박힌 것인지 의심했으나 **디자인 요소가 맞다** —
신문 지면 느낌을 내려고 깐 질감이다 (2026-08-19 확인). 손댈 것 없다.

### 영상을 넣을 때 지킬 것

1. **썸네일 지연 로딩 (lite-embed 패턴)** — 처음엔 썸네일 + 재생 버튼만 두고, 클릭할 때 iframe을 삽입한다. 유튜브 iframe은 하나당 수백 KB를 끌어오므로 이걸 안 하면 페이지가 눈에 띄게 느려진다.
2. **`youtube-nocookie.com` 도메인 사용** — 재생 전까지 추적 쿠키를 심지 않는다.

일부공개는 완전한 비공개가 아니다 — 링크가 유출되면 누구나 볼 수 있다.

## 5. Playgrown 케이스 스터디 (☕ 카페)

**원칙은 §4와 같다 — PDF 원본을 그대로 웹에 올리지 않는다.** 다만 결론이 반대다.
홈 카탈로그는 원본이 이미 옳은 형태여서 제자리에 놓기만 하면 됐지만,
**이건 원본을 해체해서 다시 써야 한다.**

### 받은 재료

19장짜리 16:9 슬라이드 덱. **파일럿 1회 — 2025-07-18 금 18–22시, 성수 북카페 센버스 대관.**

| 묶음 | 쪽 | 내용 |
|---|---|---|
| 기획 | 1–10 | 표지 · 개요 · 시작 배경 2장 · 레퍼런스 · 포지셔닝 3장 · 슬로건 · 18일 타임라인 |
| 실행 | 11–16 | 대관 공간 · 공간 조감도(5존) · 마케팅 · 가격·F&B·이용권 · 안내물·설문지 · 현장 스케치 |
| 회고 | 17–19 | 사업계획서 관점 자기첨삭 · 교훈 · 인사이트 메모 |

[ITEMS.md](ITEMS.md)가 필요하다고 적은 재료 넷 중 셋이 이 안에 있다 —
현장 사진(16쪽), 왜 시작했나(3·4쪽), 무엇이 어려웠나(18쪽).

### 이건 실패담이 아니라 파일럿 보고서다

**가장 중요한 프레이밍이다.** 2026-08-15에 확인된 사실 둘이 문서의 성격을 바꿨다.

1. 2025-07-18 하루는 **파일럿 1회**였다. 상시 운영을 하다 접은 것이 아니다
2. 접은 이유는 **사업성을 다시 설계해야 한다는 판단**이다.
   개인 사정도 있었으나 **문서에는 쓰지 않기로 했다** (2026-08-21, 쓰는 사람)

그래서 이 문서는 "잘 되다 망한 이야기"도 "하다 만 이야기"도 아니다.
**파일럿은 답을 내면 성공한 것이다.** 열어 봤고, 사람이 왔고, 설문을 받았고,
그 결과로 "이 구조로는 확장하지 않는다"는 결론을 냈다. 그게 파일럿이 하는 일 전부다.

이 프레이밍을 잡으면 §1이 이 항목에 기대한 값이 정확히 나온다 —
**끝까지 해냈는가**에 더해 **접을 줄 아는가**까지 답한다. 접는 판단은 벌인 판단보다 드물다.

읽는 사람이 "왜 지금은 안 하지?"를 궁금해할 자리에 답이 먼저 놓여 있어야 한다.
그 답이 없으면 독자는 가장 나쁜 쪽으로 짐작한다.

### 숫자 — 파일럿이라 기준이 달라진다

파일럿의 성적표는 매출이 아니라 **무엇을 알아냈는가**다. 그래서 애초에 매출 숫자가
없어도 문서가 성립한다. 다만 **"사람이 실제로 왔다"는 최소한의 사실**은 있어야
그다음 문장들이 근거를 얻는다.

받아야 할 것 (아는 만큼만. 추정치는 추정치라고 쓴다):

- **방문자 수** — 그날 몇 명이 왔나. 대략도 좋다. 이 하나가 없으면 나머지가 다 떠 있다
- **설문 응답 수와 3항목 평균** — 만족도 · 재방문 의향 · 추천 의향. **파일럿의 본 산출물이다**
- **"사업성을 손봐야 한다"는 판단의 근거** — 무엇을 보고 그렇게 정했나.
  객단가가 낮았나, 회전이 안 됐나, 인건비 구조가 안 맞았나, 콘텐츠 운영 부담이 컸나.
  **여기가 이 문서에서 가장 값나가는 두세 문장**이다
- **인스타 반응** (선택) — 팔로워, 릴스 조회수, 예약 문의 수

없는 숫자는 **없다고 쓴다.** "몇 명이 왔는지 세지 않았다"도 18쪽 교훈(「행정가가 필요」)과
곧장 이어지는 사실이라, 숨기는 것보다 적는 편이 낫다.

> **2026-08-23에 정해졌다 — 숫자는 안 넣는다** (다원님). 「없으면 없다고 쓴다」는
> 위의 규칙보다 **안 쓰기로 한 결정이 앞선다.** 판단의 근거는 받았고
> (「체험 동선 설계 재수정 필요」) 그것만 문서에 있다. 아래 「마무리」 절 참고.

### 형태 — 넘기는 화면으로 간다 (2026-08-21 뒤집힘)

**원래 판단은 「뷰어가 아니라 스크롤 페이지」였다.** 이유가 셋이었다 — 인과로 이어진
글이라 순서대로 읽혀야 하고, 원본 19장 중 여러 쪽이 문장 몇 줄에 여백이며,
같은 인터랙션이 두 구역에 있으면 마을이 단조로워 보인다. 부수 효과로 **검색엔진과
스크린리더가 읽는 유일한 항목**이 된다는 것도 컸다.

**둘을 실제로 지어서 나란히 놓고 골랐다.** A안(굴리는 문서, 방마다 바탕이 바뀜)과
B안(넘기는 화면, 세 절 견본)을 같은 주소 옆에 두고 견줬고, **쓰는 사람이 B를 골랐다**
(2026-08-21). 검색으로 들어올 사람이 없다는 판단이었다. 대가를 알고 고른 것이므로
그대로 간다.

그래서 지금은 이렇다.

- **화면 아홉을 한 장씩 넘긴다** — 표지 + 절 여덟. 화살표 · 좌우 키 · 손가락 쓸기 · 점
- **절 사이는 넘기고 절 안은 굴린다.** 일력 열여덟 장은 폰에서 절대 한 화면에
  안 들어간다 — 줄이면 글자가 안 읽히고 잘라내면 내용이 준다. 「안 굴린다」는 애초에
  가능한 선택지가 아니었다
- **JS가 없으면 그냥 굴리는 문서다.** `.viewer`를 붙이는 것이 JS라, 없으면 규칙이
  하나도 안 걸리고 아홉 화면이 위에서 아래로 이어진 한 장으로 남는다.
  넘기는 쪽을 골랐어도 **못 넘기는 상황에서 통째로 안 읽히는 일은 없어야 한다**
- **A안의 「방마다 바탕」은 살아남았다.** B로 가면서 버릴 뻔했는데, 넘길 때마다
  바닥이 통째로 바뀌니 오히려 넘어간 것이 더 또렷하다

**굴리는 문서라면 공짜였을 것 넷을 손으로 만들었다** — 넘길 때 초점 옮기기,
뒤로 가기에 화면 이름표 남기기, 스크린리더에 「9개 화면 중 5」 알리기,
안 보이는 화면을 훑기에서 빼기(`inert`). 없으면 키보드로 읽는 사람이 안 보이는
화면에 갇히고, 뒤로 가기는 마을로 나가 버리고, 눈으로 안 보는 사람에게는
아무 일도 안 일어난 것이 된다. **이것이 B의 실제 값이다.**

### 구성 — 배경 → 카테고리 → 18일 → 산출물 → 그날 → 접었다

원본 19장을 순서대로 옮기지 않는다. 아래가 읽는 순서이고, **덱의 어느 쪽을 어디에 쓰는지**의
대응표다.

**절마다 형식이 다르다.** 같은 조판으로 여섯 번 이어 쓰면 아무리 내용이 좋아도 긴 글로만
읽힌다. 각 절을 **그 내용이 원래 살았을 법한 물건의 꼴**로 만든다 — 배경 트렌드는 기사로,
카테고리 주장은 보드로, 18일은 일력으로. 읽는 사람은 글을 읽는 게 아니라
**아카이브 상자를 하나씩 꺼내 보게** 된다.

| 절 | 제목 | 형식 | 재료 |
|---|---|---|---|
| 0 | 히어로 | 표제 + 사실 넉 줄 | 1쪽 까마귀 일러스트 |
| F | **왜 지금 어른이 놀 곳이 없나** | **뉴스 기사** | 3 · 4쪽 |
| ＊ | **성인 키즈카페라는 새 카테고리** | **포지셔닝 보드** | 6 · 7 · 8쪽 |
| B | **18일 타임라인** | **일력** | 10쪽 |
| E | **18일이 낳은 것들** | **브랜드 보드** | 11–15쪽 |
| C | **실제 하루** | **방문자 동선** | 12 · 15 · 16쪽 |
| D | **접었다** | **타이핑 노트** | 18 · 17쪽 + 신규 |

0절 히어로는 남긴다. **파일럿 1회라는 사실이 첫 화면에 있어야** 뒤의 모든 문장이
제 무게로 읽힌다. 「배운 것」은 따로 두지 않고 **D에 합친다** — 접은 이유와 배운 것은
같은 정서라 한 노트에 이어 쓰는 편이 자연스럽고, 절이 하나 줄어 리듬도 낫다.

### 형식마다 지킬 것

**F · 뉴스 기사** — 헤드라인 · 리드 · 2단 조판 · 인용 박스.
내용이 원래 트렌드 논거라 형식과 정확히 맞는다. 다만 **기사 형식은 「객관적 사실」의 권위를
빌리는 꼴**이라 위험도 같이 온다.

- **실존 매체 이름 · 로고 · 기자 바이라인을 쓰지 않는다.** 없는 기사를 지어낸 것처럼 보이면
  문서 전체의 신뢰가 무너진다. 발행처는 PlayGrown 자신으로 두거나, 「배경을 기사 꼴로
  정리했다」는 것이 첫눈에 드러나게 한다
- 인용은 「공개 전 정리」대로 **요지만 내 문장으로 옮기고 출처만 적는다**.
  유튜브 댓글 캡처와 인터뷰 원문을 그대로 싣지 않는다

**＊ · 포지셔닝 보드** — 스크롤 전체에서 **유일한 도표**라 리듬상 이 자리가 좋다.
8쪽에 이미 4열 비교표(공방·클래스 / 카페·코워킹 / 소모임 / PlayGrown)가 있어
**덱에서 형식까지 그대로 살릴 수 있는 유일한 재료**다.
2×2 맵으로 다시 그린다면 축은 덱의 논리를 따른다 — 가로 `정해진 커리큘럼 ↔ 내 방식`,
세로 `혼자 ↔ 어울림`. PlayGrown이 오른쪽 위에 혼자 놓인다.

**B · 일력** — 일력은 원래 한 장씩 뜯는 물건이라 18일을 다 보여주려면 결정이 필요하다.
**스크롤에 맞춰 넘기지 않는다** — 스크롤 하이재킹은 접근성에 나쁘고, 이 문서는 인과로
이어지는 글이라 넘김이 흐름을 끊는다(§5 「형태」와 같은 이유).

→ **낱장 18개를 격자로 늘어놓되 뜯긴 자국(미싱선 · 고리 구멍)을 살린다.**
그러면 채워진 날과 빈 날이 한눈에 들어와 **준비 밀도가 그림으로** 읽힌다.
§7에서 카드 줄 길이로 밀도를 보여준 것과 같은 방법이다.

**E · 브랜드 보드** — 이 문서에서 **속도의 증거가 가장 세게 서는 자리**다.
18일 안에 나온 것이 덱에서 확인되는 것만 열두 가지다.

> 로고 · 슬로건 · 마스코트 Croww · 공간 조감도 5존 · 가격 정책 4단계 · F&B 메뉴판 ·
> 스페셜 이용권과 방문 쿠폰 실물 인쇄 · 오프라인 키링 전단 · 인스타 계정과 릴스 ·
> 모루인형 만들기 안내물 · 설문지 · 스폰서십 제안서 · 대관계획서

타일 격자로 깔고 **색 칩(로고의 빨강 · 검정)과 슬로건을 함께** 둔다.
B(18일)와 E(열두 가지)가 붙어 있는 게 핵심이다 — 기간과 산출 목록을 나란히 두면
속도가 주장이 아니라 목록이 된다. **말하지 않고 세게 하는 것**, 이 사이트가 §7에서 쓰는 방법이다.
포켓몬 전단은 「공개 전 정리」대로 잘라 쓴다.

**C · 방문자 동선** — 12쪽 조감도 위에 경로를 얹고, 지점마다 그날 사진을 붙인다.
`입장 → 온보딩 테이블 → 존 선택 → 체험 → 스탬프 → 설문 → 퇴장`.

이건 덤이 아니다. **17쪽에서 스스로 「방문자 flow 예시가 있어야 한다」고 적어 둔 것**을
여기서 실제로 만드는 셈이다. 계획서에서 비었다고 적은 칸을 케이스 스터디가 채운다.
공간과 그날이 한 화면에서 겹치는 유일한 절이기도 하다. 얼굴은 원본으로 다시 확인한다.

**D · 타이핑 노트** — **가장 조심할 절이다.** 문서에서 가장 냉정해야 할 자리인데
타이핑 연출은 가장 감정적인 형식이다. 잘못하면 신파가 된다.

→ **타이핑 애니메이션을 넣지 않는다.** 서식 없는 고정폭 글자로만 둔다.
앞의 다섯 절이 기사 · 보드 · 일력 · 브랜드보드 · 동선으로 잔뜩 차려진 뒤
**마지막에 꾸밈이 전부 사라지는 대비** 자체가 「접었다」의 정서다. 연출로 만들 것을 구조로 만든다.
덤으로 「동작 줄이기」 문제도 생기지 않는다 — 이 사이트는 이미 애니메이션을 상시 재생하기로
정해 뒀으므로(§9) 움직이는 것을 더 늘리는 데는 신중해야 한다.

내용은 **쓰는 사람이 준 세 줄뿐이다** (2026-08-21 결정).
사업성 판단도 개인 사정도 넣지 않는다 — 남이 대신 쓴 회고 문장이 한 줄이라도 섞이면
그 절 전체가 남의 말이 된다. 표제 한 줄과 세 줄이 전부다.

18쪽 교훈 세 줄(「좋아하는 것과 사업을 하는 것은 다르다」 · 「행정가가 필요」 ·
「지속과 보완을 늘 생각하기」)은 그대로 쓸 만하다. 17쪽은 사업계획서 여덟 항목마다
무엇이 빠졌는지를 스스로 적어 둔 것이라 **자기 기획의 구멍을 아는 사람이라는 증거**로
읽힌다. 다만 남에게 보이려고 쓴 글이 아니므로 통째로 옮기지 않고 몇 개를 골라 다시 쓴다.

19쪽 인사이트는 쓰지 않는다. 정리되지 않은 메모인 데다, 정리하면 남의 말과 내 말이 섞여 있어
어디까지가 내 생각인지 구분되지 않는다.

### 여섯 형식이 따로 놀지 않게

형식을 절마다 바꾸는 것의 대가는 **산만함**이다. 묶는 장치를 처음부터 정해 둔다.

- **바탕은 하나.** 배경 · 여백 리듬 · 본문 타이포 스케일 · 본문 폭을 절마다 바꾸지 않는다.
  바뀌는 것은 그 위에 **놓인 물건**뿐이다 — 같은 책상에 기사, 보드, 일력을 차례로 올리는 셈
- **절마다 한 줄 표제**를 같은 자리에 둔다. 형식이 달라도 길잡이는 같은 곳에 있어야 한다
- 형식은 **거들 뿐 읽는 순서를 바꾸지 않는다.** 가로 스크롤 · 스크롤 납치 · 자동 전환을 쓰지 않는다
- 만드는 순서는 **값이 큰 것부터** — `D 노트 → E 브랜드 보드 → B 일력 → ＊ 보드 → C 동선 → F 기사`.
  D와 E만 있어도 문서는 성립한다. C(조감도 위 좌표 작업)와 F(기사 조판)가 가장 비싸므로 뒤에 둔다

### 공개 전 정리

인쇄물·현장 배포와 웹 공개는 다르다. §4가 「네모의 꿈」 가사에서 이미 한 판단을 여기에도 적용한다.

| 대상 | 쪽 | 처리 |
|---|---|---|
| 포켓몬 오박사 전단 | 13 | 남의 저작물이다. **아이디어는 글로 설명하고 이미지는 키링 쪽만 쓴다.** 전단 실물 사진을 쓰려면 캐릭터가 안 보이게 자른다 |
| 설문지 실물 사진 | 15 | 응답자의 필체 · 나이 · 성별이 그대로 읽힌다. **텍스트로 옮겨 인용한다** — 읽기도 쉽고 검색에도 잡힌다 |
| 현장 사진 속 얼굴 | 16 | 덱에선 가려 뒀으나 **원본 파일로 다시 확인**한다. 방문객 초상권 |
| 유튜브 댓글 캡처 · 인터뷰 인용 | 4 | 제3자 글의 재게시다. **요지만 내 문장으로 옮기고 출처만 적는다** |
| 센버스 상호 · 지도 | 11 | 대관처다. 공개해도 무방하나 "대관해 하루 열었다"는 사실이 분명히 드러나게 쓴다 |
| **개인 사정** | — | 아래. **아예 쓰지 않는다** |

**개인 사정은 쓰지 않는다** (2026-08-21 결정). 한 문장만 넣는 선택지를 놓고 재 봤고,
쓰는 사람이 빼는 쪽을 골랐다.

이 사이트는 링크드인을 대신하므로 채용 담당자가 읽는다고 봐야 한다. 민감정보는
자세히 쓸수록 읽는 사람의 판단에 끼어들고, **한 문장이어도 끼어든다.**
종료 사유를 사업성 판단만으로 써도 글은 완결되므로 넣을 이유가 없다.

**이 저장소의 문서에도 같이 적용된다.** `wrangler.jsonc`의 `assets.directory`가 `.`라
**저장소 루트가 통째로 게시된다** — `PLAN.md`도 `ITEMS.md`도 주소만 알면 누구나
받아간다. 그래서 기획 문서에 적어 두었던 것도 지웠다.
다만 **git 이력에서까지 사라지지는 않는다.**

### 구현

- **경로** — `/case/playgrown.html`. HTML에 본문을 그대로 박은 정적 페이지 (`/list.html`과 같은 방식)
- **`services.json`** — `type: "doc"`, `open: "page"`, `url: "/case/playgrown.html"`.
  `open`에 값이 하나 더 생긴다 (지금은 `book`뿐)
- 카페 구역은 항목이 둘이 되므로 `direct` 없이 팝오버로 연다 (지금 그대로)
- **이미지** — `assets/case/playgrown/`. 현재 배포가 3.5MB이므로 긴 변 1600px · JPEG로 맞춘다.
  E(브랜드 보드)와 C(동선)가 이미지를 가장 많이 먹으므로 **여기서 새는지 먼저 본다**
- **CSS는 이 페이지 전용 파일로 나눈다** (`assets/case/playgrown.css`).
  형식이 여섯이라 규칙이 꽤 늘어나는데, 마을 한 채를 위한 규칙을 `styles.css`에 섞으면
  이후 마을 손볼 때마다 같이 읽어야 한다
- **일력 · 보드 · 동선은 이미지가 아니라 HTML로 짠다.** 통이미지로 만들면 편하지만
  이 페이지를 스크롤 문서로 정한 이유(검색엔진 · 스크린리더가 읽는다, §5 「형태」)가 사라진다.
  사진만 이미지고 구조와 글자는 마크업이어야 한다
- **영상** — 넣기로 하면 §4 「영상을 넣을 때 지킬 것」 둘(썸네일 지연 로딩 · `youtube-nocookie.com`)을 따른다
- `sitemap.xml`에 추가하고 `python3 tools/build_list.py`를 돌린다
- 원본 PDF · 편집 파일은 레포에 두지 않는다

### 자리

☕ **카페** 유지. 카페가 "사람들이 모여 노는 곳"이고 Playgrown이 어른들의 키즈카페라 결이 맞는다.
아니그래서(팝업 지도 앱)와 나란히 놓이면 **성수라는 동네가 두 항목의 공통 배경**이 되어
구역에 이야기가 하나 생긴다.

### 한 번 지었다 내렸다 (2026-08-20)

`/case/playgrown.html`을 재료가 있는 절부터 지어 올렸다가 **같은 날 내렸다.**
숫자가 없어서가 아니다. **절이 「물건」이 아니라 「문단」이었기 때문**이다.

§5는 절마다 형식을 다르게 해서 **아카이브 상자를 하나씩 꺼내 보게** 만들자고 했는데,
지어 놓고 보니 상자가 아니라 소제목 붙은 긴 글이었다. 포지셔닝은 표였고 산출물은
목록이었고 동선은 번호 매긴 문단이었다. 형식이 「거들 뿐」인 게 아니라 아예 없었다.

그래서 **하나의 전시 세션으로 다시 짓는다** (2026-08-20 결정). 파일은 지웠고,
같은 자리(`/case/playgrown.html`)에 새로 짓는다. 이전 것에서 가져다 쓰는 것은 없다.

### 전시 세션 — 여덟 개의 방 (2026-08-21 갱신)

읽는 순서는 그대로다. **바뀌는 것은 각 절이 화면에서 무엇이 되느냐**다.

> **리서치 메모 → 포지셔닝 보드 → 일력 → 브랜드 보드 → 마케팅 →
> 맵 & 방문자 동선 → 파일럿 → 타이핑 노트**

**여섯이 여덟이 됐다.** 재료를 받아 보니 마케팅(영상)과 파일럿(당일 영상)이 따로
있었다. 둘 다 영상이라 성격이 같고, 끼워 넣으면 흐름이 오히려 또렷해진다 —
기획(1–4) → 알림(5) → 공간(6) → 그날(7) → 판단(8). **파일럿이 노트 바로 앞에
오는 것이 특히 맞다**: "무엇을 보고 접기로 했나"의 그 「무엇」이 앞 화면에 있게 된다.

**전시감은 인터랙션이 아니라 물건의 밀도에서 나와야 한다.** §5가 이미 가로 스크롤 ·
스크롤 납치 · 자동 전환을 금지해 뒀다(인과로 이어지는 글이라 순서를 흔들면 안 되고,
접근성도 깨진다). 그러니 절마다 진짜 물건 하나씩을 놓고, 그 물건이 화면을 채우게 한다.

### 구현 기술 검토 (2026-08-20)

| 절 | 기술 | 비용 | 막힌 재료 |
|---|---|---|---|
| 칼럼 | CSS `columns: 2` · 인용 박스 `break-inside: avoid` | 중 | 덱 3·4쪽 논거 본문 |
| 포지셔닝 보드 | CSS Grid 2×2 + 축 라벨 (한 번 지어 검증됨) | 하 | 8쪽 4열 표의 값 |
| **일력** | `clip-path` 톱니 + `repeating-linear-gradient` 미싱선 + 고리 구멍 | 중 | **10쪽 날짜별 한 줄** |
| 브랜드 보드 | 타일 격자 + 라이트박스 | 중 | 11–15쪽 이미지 · 로고 색값 |
| **맵 & 동선** | 조감도 위 % 좌표 + 인라인 SVG `<path>` | **상** | 12쪽 조감도 · 16쪽 사진 · 존 이름 |
| 타이핑 노트 | 고정폭 · 애니메이션 없음 | 하 | 사업성 판단 근거 |

**일력은 실물로 확인했다.** 이미지 한 장 없이 순수 CSS로 낱장이 나온다 —
뜯긴 아랫변은 `clip-path: polygon()` 톱니, 테두리는 `filter: drop-shadow` 4방향
(clip-path가 border를 같이 잘라내므로 border를 못 쓴다), 고리 구멍은 `::before` 하나에
`box-shadow`로 복제. **다만 낱장에 종이색을 깔아야 톱니가 읽힌다** — 흰 종이를 흰
배경에 놓으면 뜯긴 자국이 안 보인다.

**맵 & 동선은 마을 지도와 정확히 같은 기법이다** — 고정 종횡비 컨테이너 + % 좌표.
좌표는 눈대중이 아니라 **원본 이미지에서 실측**한다(§6 목차 히트 영역과 같은 방법).
경로선은 인라인 SVG를 이미지 위에 절대배치하고 `aria-hidden`으로 감춘 뒤,
**지점은 실제 `<ol>`로도 존재**하게 한다 — 크롤러와 스크린리더는 SVG 선을 못 읽는다.
폰에서 조감도가 390px로 줄면 라벨이 안 읽히므로 §6이 겪은 문제를 그대로 겪는다.
누르면 확대하거나, 세로에서는 지도와 목록을 위아래로 떨어뜨린다.

**칼럼은 기술보다 위험이 크다.** 2단 조판은 쉽지만 기사 형식은 「객관적 사실」의 권위를
빌리는 꼴이다. 실존 매체 이름 · 로고 · 기자 바이라인을 쓰지 않는다(위 「형식마다 지킬 것」).

### 재료를 열어 보니 (2026-08-21)

**기술 검토가 틀린 데가 둘 있었다.** 덱을 글로만 읽고 세운 계획이라, 실물을
열어 보고서야 알았다.

| 절 | 계획했던 것 | 실제 재료 | 그래서 |
|---|---|---|---|
| 칼럼 | 2단 신문 조판 | **찢어 붙인 노트지** — 마스킹테이프 · 고리 구멍 · ✔ 불릿 · 인터뷰 인용 박스 | 「실존 매체 흉내」 위험이 **저절로 사라졌다.** 기사가 아니라 리서치 메모다 |
| 포지셔닝 | CSS Grid 2×2 + 축 라벨 | **방사형 지도** — 가운데가 우리, 둘레에 위성 여덟 | 정사각 상자 + % 절대배치. 마을 지도와 같은 기법이라 새로 배울 것이 없다 |

브랜드 보드는 기대보다 값진 재료였다. **색값이 보드에 적혀 있다** —
PLAY RED `#E63B2E` · CROW BLACK `#111111` · PAPER `#F6F3ED` · STONE `#D9D6D0`.
페이지 팔레트를 여기서 뽑았으므로 이 문서에는 눈대중으로 고른 색이 없다.
그리고 **마을 카페 앞의 까마귀 Croww가 이 브랜드의 마스코트였다** — 「까마귀가
이 기록의 등장인물」이라는 §5의 말이 문자 그대로 맞았다.

**공개 전 정리에 두 줄이 늘었다.**

- 일력 참고용으로 받은 사진은 **남의 앨범 아트**(김성규 싱글)다. 보고 만드는
  참고로만 쓰고 **사이트에는 올리지 않는다.** 어차피 일력은 순수 CSS라 필요 없다
- 존 버튼 그림에는 **제품 사진**(VR 헤드셋 · 패드 · 마사지기 · 타로 카드)이 들어 있다.
  통이미지로 깔지 않고 **품목은 글자로** 쓴다. 「HTML로 짠다」가 그대로 답이 된다

### 여덟 방을 다 지었다 (2026-08-21)

`/case/playgrown.html` · `assets/case/playgrown.css` · `assets/case/playgrown.js`.
회귀 테스트 26개가 이 절들을 지킨다(`node tests/smoke.mjs 케이스`).

| 절 | 무엇이 됐나 |
|---|---|
| 리서치 | **받은 그대로 한 장** (원본 메모지 사진) |
| 포지셔닝 | **받은 그대로 한 장** |
| **18일** | **낱장 열여덟 장을 여섯 칸 세 줄로 깐다.** 세로로 이으면 할 일 목록이 되는데, 격자에 깔면 **기간이 부피로 보인다.** 열여덟째 장만 빨갛다 |
| 브랜드 | **받은 그대로 한 장**. 페이지 색값은 이 보드에 적힌 그대로 |
| **마케팅** | 재생 카드 → 누르면 `youtube-nocookie` |
| 공간 | 조감도 위 % 좌표로 존 다섯 |
| **파일럿** | 같은 재생 카드 (빨강) |
| **노트** | 고정폭·연출 없음. **이 절에만 `.stage`가 없다** — 등장 연출이 아예 안 걸린다 |

**절 네비게이션은 아래 페이저가 대신한다.** 오른쪽 점 여덟 개는 굴려서 읽을 때
(JS 없을 때)의 목차로 남아 있다.

**영상은 재생 카드를 직접 그렸다.** 유튜브 썸네일을 깔면 페이지를 열기만 해도
그쪽에 요청이 나간다. 브랜드 색으로 그린 카드를 두고 **누른 뒤에야** 유튜브를
부른다 — §4의 「썸네일 지연 로딩」보다 한 걸음 더 간 셈이고, 검은 카드가 이 문서의
다른 물건들과 같은 결로 읽히는 덤도 있다.

**결정 셋 (2026-08-21, 쓰는 사람)**

- **동선은 넣지 않는다.** 존 다섯을 보여주는 것으로 충분하다고 판단했다.
  C절의 「경로 + 지점별 사진」은 안 만든다
- **「핫존」이 맞다** (버튼 그림의 「헛존」은 오기)
- 마케팅 영상은 **유튜브 링크**로 (mp4 5.5MB를 저장소에 두지 않는다)

### 열었다 (2026-08-21)

셋을 같이 풀었다 — `noindex` 한 줄을 떼고, `sitemap.xml`에 올리고,
`services.json`에 항목을 더해 **카페 앞 까마귀가 제 기록으로 가는 문**이 됐다.
셋이 어긋나면 회귀 테스트가 잡는다(「문 셋이 같이 열린다」).

- **`open: "page"`는 안 만들었다.** §5가 「`open`에 값이 하나 더 생긴다」고 적어
  뒀지만, 열어 보니 `app.js`가 이미 `item.url`을 그대로 링크로 쓴다.
  코드가 없는 값을 데이터에 적어 두면 나중에 읽는 사람이 없는 규칙을 찾는다
- 임시로 세워 뒀던 **까마귀의 칼럼(`mascot.column`)은 걷었다.** 그 글은 이제
  문서 첫 절에 있다 — 같은 것을 두 군데서 읽게 두지 않는다

### 마무리 — 문서가 사람을 말하게 한다 (2026-08-23)

지어 놓고 보니 **「잘 만든 브랜드 룩북」이지 케이스 스터디가 아니었다.** 읽을 수
있는 글자가 목차와 일력 열여덟 칸을 빼면 500자 남짓이었고, 그 500자 안에
「이 사람이 무엇을 하는 사람인가」와 「왜 접었나」가 둘 다 없었다. 넷을 정리했다.

- **「파일럿 1회」를 표지에 되돌렸다.** 2026-08-22에 표지의 사실 표를 걷을 때
  같이 지워진 것인데, **의도가 아니라 사고였다.** 그 한 줄이 없으면 읽는 사람은
  여덟째 화면까지 계속 운영한 가게인 줄 알고 읽고, 마지막 절의 「접었다」가
  파일럿 보고가 아니라 실패담이 된다 — 이 문서가 뒤집히는 자리다.
  표를 되살리는 대신 **줄 하나**로 되돌렸고, 회귀 테스트로 못 박았다
  (「케이스 스터디 — 표지가 말하는 것」)
- **역할 한 줄을 넣었다** — 「기획 · 브랜딩 · 공간 · 마케팅 · 현장 운영 전부」.
  이 사이트는 링크드인을 대신하는데 그때까지 다섯 중 무엇을 했는지가 **어디에도
  없었다.** 다원님이 「전체 다」라고 답했으므로 나열하고 「전부」로 닫는다.
  **「혼자」라고는 안 쓴다** — 답은 「전체 다」였지 「혼자」가 아니었다
- **접은 이유를 파일럿 절 끝에 넣었다** — 「체험 동선 설계를 다시 손봐야 한다」.
  위의 「숫자」 절이 객단가 · 회전 · 인건비 중 무엇이냐고 물었는데 **셋 다
  아니었다.** 받은 것이 한 줄이므로 한 줄만 쓴다. 그럴듯하게 살을 붙이면
  그 순간 다원님의 판단이 아니라 대신 쓴 사람의 판단이 된다
- **숫자는 안 넣기로 했다** (다원님). 위의 「숫자」 절이 「없으면 없다고 쓴다」고
  적어 두었지만, **안 쓰기로 한 것도 안 쓴다** — 모르는 것을 지어내지 않는 것과
  같은 규칙이다. 파일럿 절은 영상 카드 한 장 + 판단 한 문단으로 선다

### 절 이름과 바탕을 쓰는 사람 말로 다시 잡았다 (2026-08-23)

다원님이 폰으로 아홉 화면을 넘겨 보고 절마다 짚어 준 것을 그대로 반영했다.
**여기서부터 절 이름은 대신 쓴 문장이 아니라 다원님 말이다.**

- **바탕** — 표지가 크래프트색(`#EAE0CB`)을 가져가고, 리서치와 포지셔닝은 둘 다
  순백이 됐다. 「방마다 바탕이 바뀐다」에서 둘이 예외가 된 셈인데, 셋째에서
  넷째로 넘어가는 것은 짙은 벽이 받으므로 바닥이 멎는 구간은 문서 전체에서
  여기 한 번뿐이다. 보드 벽을 순백으로 하면 원의 옅은 색이 묻힐 것을 걱정했지만,
  그림이 제 카드 위에 크림색 바탕을 데리고 앉아서 경계가 살아 있다
- **절 이름** — 「뜯어낸 열여덟 장」→「18일의 기록」, 「18일이 낳은 것들」→
  「Playgrown의 모습」, 「15초로 알렸다」→「인스타 홍보 영상」. 라벨도 같이
  바뀌었다(「18일」→「25.07.01 – 18」, 「브랜드」→「브랜드 보드」,
  「마케팅」→「온라인 마케팅」)
- **해설 문단 둘을 걷었다** — 일력 밑의 「첫 주는 정하는 일…」과 마케팅 밑의
  「채널 하나에 다 걸지 않았습니다…」. 둘 다 그림이 이미 하고 있는 말을 글로
  한 번 더 한 것이었다. **말하지 않고 보여주는 것**이 이 사이트가 §7에서 쓰는
  방법인데, 설명을 붙이는 순간 그 값이 깎인다. 마케팅 영상의 날짜 설명
  (`figcaption`)도 같은 이유로 걷었다
- **노트 절은 라벨과 표제가 같은 말을 두 번 했다.** 「노트 / 배운 것」에서
  라벨을 걷고 표제만 남겼다. 다만 그 표제를 큰 글씨로 두면 목록보다 표제가
  세지므로 **이 절에서만 표제를 라벨 꼴로 그린다**(`.room-title--label`).
  걷은 것이 라벨이고 남긴 것이 표제인 이유는 **목차의 점과 `aria-labelledby`가
  표제를 가리키기 때문**이다 — 반대로 하면 이 절만 표제 없는 절이 된다

절 표제가 바뀌면 「JS 없이도 여덟 절의 본문이 읽힌다」가 찾는 표식도 같이
바뀌어야 한다. 그 김에 표식을 **절마다 그 절에만 있는 말**로 갈았다 —
전에는 「파일럿」처럼 표지에도 있는 낱말이 섞여 있어서, 절 하나가 통째로
빠져도 시험이 통과할 수 있었다.

**루트 원본은 파일만 지웠다** (다원님). 배포에서는 `.assetsignore`로 빼 뒀지만
저장소가 공개라 GitHub으로는 여전히 받아졌고, 칼럼 원본에는 인터뷰이의 실명과
나이가 남아 있었다. 이력은 그대로 두므로 커밋을 뒤지면 나온다 — 거기까지 지우려면
두 브랜치를 다시 써야 해서 하지 않기로 했다. **`.assetsignore`의 `/[0-9].*`는
남긴다** — 다원님이 그림을 올리는 자리가 저장소 루트라, 규칙이 없으면 다음에
올리는 원본이 곧장 배포된다.

### 뼈대 — 여덟 방을 하나로 묶는 것

- **한 페이지 유지.** 전시관 하나를 통으로 걷는 것이 맞고 검색도 한 장에 모인다
- **절 네비게이션은 미정.** 전시라면 「지금 몇 번째 방인지」가 보여야 하는데 스크롤 납치는
  금지다. 이 사이트 어휘로는 §6의 챕터 바가 전례다 — 오른쪽에 조용한 세로 점 여섯 개
  (`IntersectionObserver`로 현재 절 표시, 누르면 앵커 이동), 데스크톱만. **쓰는 사람이 정한다**
- **등장 연출은 `animation-timeline: view()`로 짧게만.** §6에서 이미 쓴 기술이고 미지원
  브라우저에서는 통째로 무시된다. **타이핑 노트만은 무조건 정적** — 꾸밈이 사라지는
  대비가 그 절의 정서다
- **이미지 예산.** 지금 배포가 3.1MB다. 격자에는 **긴 변 800px 썸네일**(~60KB)만 깔고
  **1600px 원본은 눌렀을 때** 온다. 이게 첫 로드를 지키는 유일한 방법이다.
  덱에서 잘라 두 벌로 뽑는 도구가 하나 더 필요하다 (`tools/cut_case.py`)
- **HTML로 짠다.** 일력 · 보드 · 동선은 이미지가 아니라 마크업이어야 한다.
  통이미지로 만들면 이 페이지를 스크롤 문서로 정한 이유가 사라진다
- **회귀 테스트** — 절 여섯 존재 · 일력 18칸 · 동선 좌표가 조감도 밖으로 안 나감 ·
  JS 없이 본문 읽힘 · 이미지가 실제로 뜸

### 만드는 순서

**노트 → 브랜드 보드 → 일력 → 포지셔닝 → 칼럼 → 맵 & 동선.**
「값이 큰 것부터」는 그대로 두고 맵 & 동선만 맨 뒤로 뺐다 — 가장 비싸다.
절이 서로 독립이므로 **재료가 오는 절부터 하나씩 올린다.**

### 마을에서는 카페 앞 까마귀로 들어온다

**마스코트 Croww가 자기 기록으로 데려간다.** 세모집 우편함과 같은 규칙이다 —
**물건마다 문이 하나씩**. 카페 건물을 누르면 팝오버가 항목을 다 보여주고,
카페 앞에 선 까마귀를 누르면 PlayGrown 기록만 열린다.
문이 갈려도 어수선하지 않은 이유는 **까마귀가 이 기록의 등장인물이기 때문**이다 —
18일 안에 나온 것 중 하나가 바로 이 까마귀다.

**지금은 문이 닫혀 있다.** 전시 세션을 다시 짓는 동안 문서가 없으므로
`mascot.item`을 비워 뒀고, 그러면 까마귀는 링크가 아니라 **풍경으로만 선다**
(`<span>` · `pointer-events: none` · 스크린리더에서 감춤). 없는 곳을 가리키는 링크를
두지 않기 위해서다. 새 페이지가 올라가면 `item`을 도로 채우면 그만이다.

- 데이터 — `districts[].mascot = { name, at, w, frames, item }`. 좌표는 발밑(`at`),
  `w`는 지도 폭 기준 %. 그림 경로를 코드에 박지 않는다
- 크기는 **"나" 캐릭터보다 조금 작게** — 폭 5.6%(캐릭터 5.4%)에 키는 그보다 낮다.
  마을에서 캐릭터가 가장 큰 생물이어야 시선이 거기 먼저 간다
- **걸음이 다르다.** 마을과 같은 2.4초 한 박자를 타되 건물은 두 번 숨쉬고, 우편함은
  한 번 크게 뛰고, **까마귀는 한 번 퍼덕이고 한 번 갸웃한다.** 같은 리듬 위의 다른
  걸음이라 같은 요소로 안 읽힌다 (§6 우편함에서 정한 방법 그대로)
- **퍼덕임과 갸웃은 그림 세 장을 번갈아 만든다.** 이 마을에는 프레임 애니메이션이 없고
  CSS transform만 쓰는데(§9), 날개가 실제로 올라가려면 그림이 여러 장이어야 했다.
  `steps(1)`이라 장 사이가 섞이지 않는다. 프레임은 순서가 아니라 **이름이 곧 역할**이다
  (`rest` · `flap` · `tilt`) — 순서에 기대면 한 장만 늘어도 어긋난다
- 세 장은 **같은 배율·같은 캔버스에 눈을 기준으로** 얹는다 (`tools/cut_crow.py`).
  장마다 제 상자에 맞춰 줄이면 갈아 끼울 때 새가 커졌다 작아지고, 부리 끝에 맞추면
  몸이 옆으로 미끄러진다 — **눈은 어느 장에서나 같은 것**이라 거기 맞추면 머리가
  제자리에 있고 날개·꼬리·자세만 바뀐다

## 6. 게임 안내서 (`/game/`)

> 게임을 여행하는 히치하이커를 위한 안내서 · 원본 46면 · 게임 37편

**한 번 폐기했다 되살린 항목이다.** 다만 그때 폐기한 설계로 되살린 것이 아니다 —
§4에서 "46쪽 게임 가이드에 플립북은 부적합하다"고 본 판단은 그대로 두고,
**챕터 네비게이션**이라는 다른 형태로 지었다. 그건 **찾는 것**이었고 이건 **고르는 것**이다.

### 원본을 한 픽셀도 고치지 않는다

§4·§5와 다른 셋째 경우다. 홈 카탈로그는 원본이 이미 옳은 형태였고, Playgrown은 해체해
다시 써야 했다. 이건 **원본은 그대로 두고 그 위에 길만 얹는** 경우다.
슬라이드 46면을 이미지로 깔고, 챕터 바·목차 히트 영역·문(門)만 새로 만든다.

### 챕터가 다섯인데 번호는 1 · 2 · 3 · 4 · 42다

챕터는 장르가 아니라 **읽는 사람의 상태**로 나뉜다. 그리고 42는 다섯 번째 챕터가 아니다 —
『은하수를 여행하는 히치하이커를 위한 안내서』에서 삶·우주·모든 것에 대한 답이 42였던 것처럼,
**찬사를 보내는 목록**이라 층위가 다르다. 그래서 번호를 5로 고치거나 순번을 다시 매기지 않는다.

UI에서도 그 점이 드러나야 하므로 **챕터 바에서 `4`와 `42` 사이를 다른 간격의 여러 배로 벌리고
그 사이에 옅은 구분 기호를 둔다** (실측 119px 대 2px). **이 간격이 이 페이지의 시그니처다.**
그래서 나머지는 전부 조용하게 둔다 — 무채색 두 단계와 종이색 하나, 그림자·라운드·그라디언트 없음,
헤어라인은 챕터 바 아래 하나뿐. 원본이 흑백 선화에 파스텔 태그 칩을 쓰므로 **크롬이 색을 더하지 않는다.**

42에는 호버 툴팁으로 한 줄만 붙인다 — `삶, 우주, 그리고 모든 게임에 대한 답`.

### 챕터마다 따로 놓인 카드 뭉치다

46면을 한 줄로 이어 두고 챕터 이동을 smooth 스크롤로 처리했더니, 챕터를 누를 때마다
**사이의 스무 장이 전부 지나가느라 화면이 파바박 튀었다.** 지금은 뭉치를 일곱으로
갈랐다 — 표지·목차 / 챕터 다섯 / 맺음말. 한 번에 한 뭉치만 화면에 올리므로
그 구간이 통째로 없어진다. **다른 책 다섯 권을 번갈아 집어 드는 것**에 가깝다.

- 뭉치를 갈아 끼울 때 스크롤 위치는 **애니메이션 없이** 잡는다. 여기서 smooth를 쓰면
  올리자마자 화면이 흐르는데, 뭉치를 가른 이유가 바로 그 흐름을 없애는 것이었다
- 새 뭉치는 0.26초 동안 짧게 올라오며 자리를 잡는다. 길면 넘기는 맛이 아니라 기다림이 된다
- 챕터 표시는 **올라온 뭉치가 곧 현재 챕터**라 `IntersectionObserver`가 필요 없어졌다
- 뭉치 끝에서 한 걸음 더 밀면(휠·스와이프) 다음 뭉치로 넘어간다. 카드 안에서는
  브라우저의 스냅 스크롤을 그대로 쓰고 **경계에서만** 우리가 받는다

### 스크롤이 아니라 카드가 넘어가야 한다

원본이 큐카드 같은 안내서라 스크롤이 「흐르는」 대신 「넘어가는」 것으로 읽혀야 한다.
스크롤 진행에 맞춰 카드가 기울고 작아지며 위로 빠지고, 다음 카드가 아래에서 반듯하게
올라와 앉는다 (`animation-timeline: view()`). 시간 기반 전환과 달리 **손가락을 되돌리면
그림도 되돌아간다** — 넘기다 만 상태가 손에 남는다. 지원하지 않는 브라우저에서는
규칙이 통째로 무시되고 지금까지의 스냅 스크롤 그대로 동작한다.

두 가지를 실물로 보고 고쳤다.

- **평평한 구간을 좁혔다.** 처음엔 38~62%를 평평하게 뒀는데, 멈추는 지점은 스냅 때문에
  언제나 정확히 50%라 그렇게 넓을 이유가 없었다. 카드가 화면에서 거의 빠진 뒤에야
  기울어져 넘기는 맛이 없었다 — 지금은 47~53%만 평평하다
- **투명도를 뺐다.** 카드도 뒤의 간지도 흰 종이라, 조금만 비쳐도 **간지의 글자가
  슬라이드를 뚫고 올라와 인쇄 사고처럼 보였다.** 카드는 끝까지 불투명하게 두고
  기울기와 크기로만 넘긴다. 간지는 카드가 비운 자리에서만 드러난다

### 붙는 것과 걸리는 것을 갈라야 한다

간지(챕터 표지)는 스티키로 붙어 있고 게임 카드가 그 위로 미끄러져 올라가며 덮는다.
챕터가 끝나는 순간 간지가 잠깐 다시 드러나고, 그 틈이 챕터가 닫히는 신호가 된다.

**처음에는 간지 카드 자체에 `position: sticky`를 주고 그것을 그대로 스냅 대상으로 삼았는데,
그게 틀렸다.** 스티키 요소는 붙어 있는 동안 스냅 위치가 스크롤을 따라 움직인다.
`scroll-snap-type: mandatory`가 그 움직이는 목표를 쫓느라 챕터 1을 눌렀는데 목차로 가는 식으로
엉뚱한 면에 섰다.

지금은 둘을 갈라 뒀다 — **`.chapter-bg`가 붙고, 투명한 `.card--divider`가 스냅을 받는다.**
배경에 음수 마진을 줘 흐름에서 자리를 차지하지 않게 하면 둘이 정확히 겹친다.
회귀 테스트가 챕터마다 간지에 정확히 서는지 지킨다.

### 마을에서는 우편함으로 들어온다

세모집 구역에 항목이 둘(홈 카탈로그 · 게임 안내서)이지만 팝오버로 묶지 않았다.
**물건마다 문이 하나씩** — 건물을 누르면 소개서가 집에서 날아오고, 마당의
**우편함**을 누르면 게임 안내서가 온다. 안내서는 만든 게 아니라 **배달된 물건**이라는
설정이 우편함과 맞고, 문이 갈리면서 두 물건이 다른 층위라는 것도 함께 읽힌다.

- 데이터 — `districts[].mailbox: { box, item }`. 상자는 지도 픽셀 실측
  (`[87.9, 94.35, 1.95, 3.15]%`), item이 이 문이 여는 항목이다
- **우편함은 링크(`<a href="/game/">`)다.** JS가 없어도, 새 탭으로 열어도 동작한다.
  링크 상자는 가만히 있고 안의 복제본만 움직인다 — 누를 자리는 고정이어야 한다는
  "나" 캐릭터의 규칙 그대로. 판정 영역은 44px 이상으로 넓혀 깐다
- **건물과 다른 걸음으로 구별한다.** 같은 2.4초 박자·같은 위상(25%)이되, 건물이
  두 번 숨쉬듯 부풀 때 우편함은 **한 바퀴에 한 번만 크게 뛴다.** 리듬을 깨지 않고
  걸음을 바꾸는 것 — 같은 요소로 안 읽히게 하는 방법이다. 호버도 각자 반응한다
- **누르면 카드 다섯 장이 우편함에서 챠라라라 쏟아진다.** 안내서가 큐카드 뭉치라
  한 장으로는 그 성격이 안 읽힌다. 뒤 넉 장은 **빈 카드**다 — 이미지를 더 받지 않고도
  뭉치로 읽히고, 도착 화면(표지)과 겹치는 얼굴이 여럿이면 오히려 어수선하다.
  뒤에서부터 던져 **겉장(표지)이 마지막에 맨 위로 내려앉고**, 그 그림 그대로 페이지가
  열린다 (`/game/?from=town`은 목차 대신 표지에서 시작).
  겉장이 350ms 안에 준비되지 않으면 그림을 기다리지 않고 그냥 이동한다

### 그 밖에 정한 것

- **첫 화면은 목차다.** 37편의 리스트가 아니라 챕터 다섯 개를 고르는 화면이어야 한다.
  원본 2면 위에 투명 히트 영역만 얹었고, 챕터 이름을 새로 타이핑하지 않았다.
  좌표는 눈대중이 아니라 **원본 이미지에서 글자 행을 실측**해 얻었다 (중심 y 28.5 · 40.8 · 53.4 · 65.6 · 80.0%)
- **문(門)** — 챕터 1·2·3·4 끝에만. 42 뒤에는 붙이지 않는다. 이미지가 아니라 유일하게 새로 만드는
  화면이라 요소를 늘리지 않는다. 진행 표시는 `2 / 5` 같은 분수가 아니라 점으로 찍고 42만
  떨어뜨린다 — 챕터 바와 같은 규칙을 반복해야 일관된다
- **마지막 두 장**(그 외 게임의 정석들 · 엔딩)은 42에 딸린 것이 아니라 덱 전체를 닫는 말이라
  **챕터 바에서 접근 경로를 주지 않는다.** 끝까지 스크롤한 사람만 만난다
- **폭 900px 미만에서는 카드를 눌러 확대**한다. 이 경로가 없으면 폰에서 축 라벨과 리뷰 캡처를
  읽을 수 없다. 확대 뷰어는 **화면 높이를 채우는 배율로 연다** — 1배로 열면 카드와 똑같이 납작해
  여는 의미가 없다. 핀치·팬·두 번 두드리기를 직접 처리한다
- 챕터 구성과 면 순서는 전부 `game/data.json`에서 읽는다. 코드에 박지 않는다
- `sitemap.xml`에 함께 올라간다 (`build_list.py`가 내부 경로를 가진 항목을 자동으로 넣는다)

### 한 면이 통째로 안 들어가고 있었다 (2026-08-23)

**넓은 화면과 가로로 돌린 폰에서 슬라이드 아래가 잘려 나갔다.** 표지의 날짜와
DADA 로고가 화면 밖으로 밀려나 있었는데, 1년 가까이 아무도 못 봤다 —
**개발하며 쓰는 1440px에서는 멀쩡해 보이기 때문**이다.

원인은 한 줄이다. 카드는 `display: grid`인데 **행 크기를 안 적으면 행이 `auto`**라
그림 높이(1080px)만큼 커진다. 카드 자신의 `height`가 904px로 못 박혀 있어도 행은
1080px이고, 그러면 그림의 `max-height: 100%`가 **제 높이를 자기 기준으로 재는 꼴**이라
아무것도 제한하지 못한다. 넘친 만큼은 그냥 잘린다.

폭이 좁을 때는 `max-width: 100%`가 먼저 걸려 세로가 남으므로 티가 안 났다.
잘리기 시작하는 경계는 `min(화면 폭, 1920) × 9/16 > 카드 높이`이고, 실제로는
**1600px 넘는 데스크톱**과 **가로로 돌린 폰**(844×390 → 131px 잘림)이 걸렸다.

행을 못 박는 방법도 있지만, 목차처럼 그림이 상자(`.toc-frame`) 안에 한 겹 더
들어가면 그 상자의 높이가 다시 `auto`라 같은 일이 되풀이된다. 그래서
**길이를 직접 준다** — `max-height: var(--card-h)`면 몇 겹으로 싸여 있든 카드
높이를 못 넘는다.

**회귀 테스트는 폭을 여덟 벌 잰다.** 한 벌만 보면 이 종류는 영영 안 잡힌다 —
고치기 전 상태로 되돌려 넷이 실패하는 것을 확인하고 넣었다.

### 공개 전 확인 — 아직 안 한 것

원본을 고치지 않는다는 원칙과 부딪히므로 **판단이 필요하다. 지금은 원본 그대로 올라가 있다.**

**리뷰 캡처의 계정명은 지우지 않는다.** 처음엔 개인정보로 보고 가리려 했는데
반대였다. 스팀 리뷰는 남이 쓴 저작물이고, 이 안내서는 그걸 **인용**하고 있다.
저작권법 §37은 인용할 때 출처를 밝히도록 하고, 저작자의 실명이나 이명이 표시돼
있으면 그것도 함께 적으라고 한다. 계정명은 감춰야 할 신상이 아니라 **그 사람이
공개적으로 쓰기로 고른 이름**이고, 지우면 오히려 출처 없는 인용이 된다.

46면을 훑어보니 계정명이 남은 면은 **21면(`SEXY BOY`)과 42면** 둘뿐이고
나머지는 본문만 잘라 썼다. 엄밀히 따지면 **문제가 있는 쪽은 이름이 붙은 둘이
아니라 이름이 잘린 나머지**다. 인용은 30면 넘게 흩어져 있다.

**그래도 전부 다시 캡처하지는 않는다.** 캡처 자체는 쉽지만 값이 안 나온다.

- **다시 찾는 것이 진짜 비용이다.** 인용된 리뷰의 게시일이 2017년 · 2022년 ·
  2023년까지 흩어져 있다. 스팀에는 「몇 해 전에 갈무리한 그 리뷰」를 되찾는 길이 없다
- **디자인 재작업이 딸려 온다.** 리뷰 상자는 지면 레이아웃에 맞춰 크기와 자리가
  잡혀 있다. 이름 줄이 더 붙으면 그 면들의 조판을 다시 맞춰야 한다
- 얻는 것은 「출처를 더 정확히」인데, 아래 한 줄로도 「출처 없음 → 출처 있음」은 넘어간다

**대신 총괄 출처를 한 줄 둔다.** §37은 출처를 「이용 상황에 따라 합리적이라고
인정되는 방법으로」 밝히라고 한다. 개인 안내서에 실린 짧은 인용 서른몇 개라면
면마다가 아니라 한 곳에 모아 밝히는 것도 합리적인 방법에 든다.

> 본문에 인용한 이용자 리뷰는 모두 Steam 상점 페이지의 공개 리뷰입니다.

엔딩 면 아래나 새 한 면에 넣는다. **기존 46면은 한 장도 건드리지 않는다.**
앞으로 리뷰를 새로 갈무리할 때는 **처음부터 이름째로 뜬다** — 그게 제일 싸다.

**넣었다 (2026-08-20) — 판권면 한 장.** 맺음말 뒤에 새로 놓는 화면이고 원본 46면은
한 픽셀도 건드리지 않았다. 문(門)에 이어 **이미지가 아닌 두 번째 화면**이라 크롬이
색을 더하지 않는다는 §6의 규칙을 그대로 따른다 — 무채색 두 단계, 헤어라인 하나,
글자는 문보다 한 단계 낮게. 챕터 바에 자리를 주지 않으므로 맺음말과 마찬가지로
**끝까지 온 사람만 만난다.**

문구는 `game/data.json`의 `colophon`에서 읽는다 (판본·날짜·지은이는 이미 있던 `meta`).
코드에 박지 않는다는 §6의 규칙이 여기에도 적용된다.

**첫 화면인 목차에도 회색 한 줄을 얹었다.** 판권면에만 두면 **끝까지 온 사람만 본다** —
출처 표시가 정작 인용을 읽는 사람에게 안 닿는다. 목차는 거의 모두가 지나는 화면이고,
원본 2면은 글줄이 86.6%에서 끝나 아래가 비어 있다. 히트 영역과 같은 방식으로
**이미지 위에 얹을 뿐 원본 파일은 그대로다.**

> 인용한 이용자 리뷰 출처: Steam 상점 페이지 공개 리뷰

- 클릭을 먹지 않게 `pointer-events: none` — 바로 아래가 챕터 히트 영역이다
- 스크린리더에는 감춘다(`aria-hidden`). 판권면이 같은 말을 온전한 문장으로 읽어준다
- 좁은 화면에서는 원본이 손바닥만 해져 지면 안에 넣으면 로고와 겹치므로 지면 바로 아래로 내린다

**「출처: 스팀」으로 줄이지 않은 이유.** 그 넉 자로도 「어디서 왔는지 안 밝힘」은
면하지만, 스팀은 상점이자 런처이자 커뮤니티라 **무엇을 인용했는지가 안 걸린다.**
`상점 페이지`가 어디인지를, `공개 리뷰`가 무엇인지를 짚어야 읽는 사람이 원본을
찾아갈 수 있고 §37이 말하는 「합리적인 방법」도 거기서 성립한다. 열 자 더 쓰는 값이다.

**판본 표기와 날짜 어긋남도 여기서 함께 받는다.** 표지에 인쇄된 `2026.07.08`은 Ver.3을
뽑은 날이고 마을 카드의 `25.02`는 엮은 때다 — 둘 다 사실인데 따로 놓여 있어 읽는
사람에게만 어긋나 보였다. 판권면에서 `Ver.3 · 2025.02 엮음 · 2026.07.08 펴냄`으로
나란히 쓰면 **어긋남이 아니라 이력이 된다.** 표지 상단 러닝 텍스트의 `Ver.2`는
이미지 안이라 그대로 남는다 — 원본을 다시 뽑을 일이 생기면 그때 함께 고친다.

| 무엇 | 어디 | 왜 |
|---|---|---|
| ~~리뷰 계정명~~ | 21 · 42면 | **그대로 둔다.** 인용의 출처 표시다 (위 문단) |
| **총괄 출처 한 줄** | 판권면 (맺음말 뒤 새 면) | ✅ **넣었다** (2026-08-20). 46면은 그대로 |
| 판본 표기 어긋남 | 표지 | 상단 러닝 텍스트는 `Ver.2`인데 본문 제목은 `Ver.3`.
  **표지 이미지 안이라 그대로 둔다** — 판권면이 `Ver.3`을 못박는다 |
| 표지 날짜와 수록 시기 | 표지 · 카드 | 표지에 `2026.07.08`이 인쇄돼 있는데 마을 카드의 만든 시기는 `25.02`다.
  둘 다 사실이다(만든 때 / Ver.3을 뽑은 때) — ✅ **판권면에서 한 줄로 나란히 세웠다** |
| 이메일 주소 | 엔딩 면 | 이미지라 수집 위험은 낮다. 다만 웹 카드 쪽에 별도 연락 경로를 두는 편이 안전하다 |

**`25.02`는 근거가 있다.** 면에 박힌 스팀 리뷰 중 가장 최근 것이 `2025년 2월 22일`
(21면)이고 그다음이 `2025년 1월 26일`(42면)이다. 그보다 이른 시점에는 이 덱을
만들 수 없으므로 2025년 2월은 앞뒤가 맞는다. 표지의 `2026.07.08`은 Ver.3을 뽑은
날이지 만든 날이 아니다.

## 7. 전체 목록

항목이 열댓 개 안팎이면 마을만으로도 탐색이 되지만, 훑어보고 싶은 사람을 위해 목록을 한 번의 조작으로 연다.

**구현 완료** — 목록 모달: 구역별 그룹 헤더 + 카드(아이콘 · 이름 · 한 줄 설명 · 타입 배지 · 만든 시기), 즉시 검색, 타입 필터 칩(개수 표시, 0개인 타입은 칩 자체가 안 생김), 바깥 클릭·ESC·닫기 버튼, 포커스 트랩.

### 목록은 만든 순서다 — 속도를 말하지 않고 보여준다

**목록 모달은 언제나 만든 순서로 늘어놓는다. 구역별 보기도, 보기 전환 토글도 두지 않는다.**

한때 「구역별 / 지어진 순서」 토글을 뒀다가 없앴다. **구역은 지도가 이미 하는 일**이라
목록까지 구역으로 묶으면 같은 분류를 두 번 보여주게 되고, 그러는 동안 정작 목록만 할 수 있는
것(시간)이 선택지 뒤로 숨는다. 기본값으로 놓아야 보는 사람이 고르지 않아도 보인다.
토글이 사라지니 「지어진 순서」라는 이름도 화면에 쓸 일이 없다 — 달 제목이 그 자리를 대신한다.

구역별 묶음은 `/list.html`에만 남는다. 마을은 구역으로 경험하고, 목록은 시간으로 읽는다.

기획·사업은 2년에 걸쳐 쌓였는데 **웹은 2026년 3~8월 여섯 달에 9개가 몰려 나왔다.**
채용·협업 관점에서 이건 값이 큰 사실이지만, "빠릅니다"라고 쓰면 곧바로 "그래서 품질은?"으로
되받힌다. 그래서 **문장으로 주장하지 않고 목록의 모양으로 둔다.**

**최근 것부터** 달 단위로 묶고, 카드 폭을 고정한다. 그러면 그 달에 몇 개를
만들었는지가 **줄 길이로** 드러난다 — 한 개인 달은 짧고 세 개인 달은 길다. 카드를 늘려
화면을 꽉 채우면 그 차이가 통째로 사라지므로 늘리지 않는다. 차트를 넣지 않는 이유이기도 하다.
항목 열댓 개 규모에서는 목록 자체가 이미 그래프고, 픽셀 마을에 축과 눈금이 들어오면 톤이 깨진다.

`/list.html`에도 같은 순서의 섹션을 함께 생성한다 — 크롤러와 스크린리더에게도 같은 사실이 보여야 한다.

마을은 공간이라 원래 시간축이 없다. 다만 **마을은 지어진다** — 목록이 그 순서를 맡는다.
순서는 **최근 것이 맨 위**다. 포트폴리오를 여는 사람이 가장 먼저 궁금해하는 것은
「요즘 뭘 하나」이지 「처음에 뭘 했나」가 아니다.

**`/list.html` — 구현 완료.** 모달 안의 내용은 검색엔진과 스크린리더가 읽지 못한다. JS를 끄고 재보니 첫 화면의 본문이 74자였고 항목 이름이 하나도 없었다 — 검색으로 닿을 길이 없었다는 뜻이다.

같은 `services.json`으로 **HTML에 내용을 그대로 박은** 정적 페이지를 만들었다(1,200자). 항목 이름·설명·약력·연락처가 전부 읽히고, `sitemap.xml`·`robots.txt`가 함께 생성되며, 모달 하단에서 링크한다. 마을은 경험, `/list`는 정보로 역할이 갈린다.

빌드 과정이 없는 사이트라 `python3 tools/build_list.py`를 손으로 돌린다. 잊으면 `npm test`가 어느 항목이 어긋났는지 짚어준다 — 단일 소스를 유지하면서 정적 산출물을 두는 값이다.

## 8. PC / 모바일 대응

**배경 이미지는 가로 1장으로 PC·모바일을 모두 커버한다.** 세로 시안은 만들지 않는다.

지도가 3열 블록이라 모바일 폭(390px)에 맞춰 축소해도 블록 하나가 약 130px, 가장 작은 한옥도 약 60px로 터치 기준(44px)을 넉넉히 넘는다. 320px 화면에서도 50px 수준이라 문제없다.

**PC에서는 스크롤이 없다.** 지도 높이를 `100dvh`에서 나머지 요소들의 높이(336px)를 뺀
만큼으로 묶어 마을 · 안내 · 버튼 · 푸터가 한 화면에 다 들어온다. 예전에는 그 값을 290px로
잡아 두어 **어느 화면에서나 딱 45px씩 넘쳤다** — 스크롤바가 하나 생기는 이유가 그것이었다.
지도 아래 요소의 높이를 바꾸면 `styles.css`의 그 숫자도 같이 고쳐야 한다.

| | PC | 모바일 |
|---|---|---|
| 지도 | 화면 폭 기준, 최대 1440px · **세로는 한 화면에 맞춤** | 폭에 맞춤, 세로 가운데 정렬 |
| 구역 열기 | 누른 건물 **바로 옆에** 팝오버 (위치는 JS가 계산) | 지도 **아래에 패널**이 흐름대로 붙음 |
| 목록 | 상단 버튼 → 중앙 모달 | 상단 버튼 → 모달, 추천 픽은 하단 시트 |
| 터치 영역 | — | 최소 44×44px 보장 |

초기 기획의 "모바일 하단 전체 카드 리스트"는 넣지 않았다. 항목이 열댓 개 안팎이라 지도를 눌러 패널을 여는 것으로 충분하고, 상시 리스트는 지도를 화면 밖으로 밀어낸다.

## 9. 기술 설계

- **데이터 단일 소스** — `services.json` 하나로 마을 팝오버 · 목록 모달 · 프로필 · 푸터를 전부 렌더링
- **지도** — 픽셀 배경(`image-rendering: pixelated`) + 구역 위치에 `<button>` 핫스팟. 고정 종횡비 컨테이너 안에서 % 좌표를 쓰므로 화면 크기가 바뀌어도 좌표가 어긋나지 않는다
- **가시성은 뾰잉 하나로 끝낸다** — 폰에서 건물 뾰잉은 5.1px, 우편함은 0.2px뿐이고
  이 지도는 자동차·새·구름·사람이 전부 움직여 그게 배경 소음에 묻힌다(실측).
  그래서 더 눈에 띄게 하는 안을 두 번 만들어 보고 두 번 다 걷어냈다.
  **① 위→아래 파도** — 줄마다 위상을 어긋내 뾰잉이 흘러내리게 했다. 움직임이 흐르기는
  해도 여전히 「무엇이 눌리는지」를 말해주지 않았다. **② 가리키는 화살표** — 문마다
  핫핑크 화살표를 하나씩 띄웠다. 가리키기는 확실했지만 열 개가 붙는 순간 마을이 아니라
  표지판이 됐다 — 지도에 없는 색을 열 군데에 찍는 건 결국 두 번째 벽지다.
  이 마을은 예전에도 물결 표식을 넣었다 건물 뾰잉으로 바꾼 적이 있는데, 화살표는
  그 물결과 같은 범주였다. **눌러볼 수 있다는 말은 지도 아래 `.hint` 한 줄이 맡는다**
- **건물은 같이 숨쉬고, 개별 요소는 저마다 다른 걸음을 갖는다** — 이게 움직임의 규칙이다.
  건물 여덟은 전부 같은 `bldg-idle`로 함께 부풀지만, 하나짜리 물건들은 서로 다르게 움직인다:
  **우편함**은 좌우로 인사하듯 또잉또잉, **확성기**는 한 박자에 두 번 앞으로 내지르고,
  **까마귀**는 퍼덕이고 갸웃, **쪽지**는 파장처럼 뽀잉, **공원 사람들**은 왼쪽부터
  오른쪽까지 파도타기, **「나」**는 손 흔들며 폴짝.
  박자(2.4초)를 공유하되 그 안에서 하는 일이 달라야 **같은 것으로 안 읽힌다.**
  쪽지만 박자까지 다른데(1초·0.55초), 마을의 일부가 아니라 마을 위에 온 것이라서다
- **애니메이션** — 자동차는 도로 구간(`CAR_ROUTES`)을 wrap/pingpong으로 순환, 새·구름은 느린 수평 이동, 건물·사람은 `folk-idle` 2.4초 한 박자. **사용자 결정으로 OS '동작 줄이기'와 무관하게 상시 재생.** 구현 주의사항은 [README](README.md)에 정리
- **방문 통계는 커스텀 이벤트가 본체다** — 이 마을은 **URL이 바뀌지 않는다.** 구역을
  열어도 책을 넘겨도 목록을 열어도 주소는 `/` 그대로라, 자동으로 세어지는 페이지뷰
  하나로는 「몇 명이 왔다」까지만 알고 **「무엇을 봤는가」는 한 줄도 안 남는다.**
  그래서 구역·항목·책·우편함·목록·안내서 챕터가 각자 신호를 보낸다 (목록은 [README](README.md)).
  붙는 자리는 `ga.js` 하나, 부르는 자리는 `window.dadaTrack()` 하나다.
  **켜지는 곳은 배포 도메인뿐이다** — localhost에서 켜면 `networkidle`에 기대는
  회귀 테스트가 남의 서버 사정에 흔들리고, 고치면서 여닫은 것이 숫자에 섞인다.
  Cloudflare Web Analytics 비콘도 같은 가드 아래에서 함께 붙인다 — Workers 정적
  에셋에는 「켜면 알아서 끼워 넣기」가 없고(그건 Pages 얘기다) `*.workers.dev`는
  Cloudflare 존도 아니라, 호스트 이름을 직접 적어 만든 뒤 토큰을 받아야 한다
- **데이터와 코드는 캐시가 어긋나면 안 된다** — 빌드가 없어 파일 이름에 해시를 못 붙이므로
  `services.json`·`app.js` 같은 「짝이 맞아야 하는」 파일만 `_headers`로 `no-cache`를 걸어
  **쓰기 전에 물어보게** 한다. 그림과 지도는 그대로 오래 캐시된다.
  한 번 데였다 — 예전 `services.json`이 새 `app.js`를 만나 카페 앞에 물음표 상자가 섰다.
  그래서 **깨진 그림은 스스로 사라지게** 해 뒀다. 없는 그림이 깨진 그림보다 낫다 ([README](README.md))
- **날아다니는 것도 문이 될 수 있다** — 마을 위를 도는 쪽지는 새·구름과 달리 눌린다.
  풍경과 갈리는 신호는 다섯이다 — 길목마다 **멈추는 걸음**, **파장처럼 커졌다 작아지는
  뽀잉**(1.8초), 마을 2.4초 박자를 타지 않는 **0.55초 펄럭임**, 새·구름보다 **위에 뜨는
  높이**, 손을 대면 **멈추는 비행**.
  한때 건물 클릭을 가로챌까 봐 **멈춰 선 동안만** 잡히게 했다가 되돌렸다 —
  한 바퀴의 5분의 1만 눌리는 물건이 됐기 때문이다. **누르려다 안 눌리는 문은 문이 아니다.**
  대신 멈추는 자리를 전부 빈 땅에 두고 판정을 그림만큼만 잡는다 ([README](README.md))
- **스택** — 프레임워크 없이 HTML + CSS + vanilla JS. 빌드 과정 없음
- **배포** — Cloudflare Workers 정적 에셋. 저장소 루트를 통째로 올리므로 **올리지 않을 것은
  `.assetsignore`에 적는다** (개발 파일이 섞여 들어가 배포가 막힌 적이 있다 — [README](README.md) 「배포」)
- **원본 PDF** — 레포에 두지 않는다

### services.json 스키마

```json
{
  "id": "grammar",
  "name": "고등영어문법",
  "district": "school",
  "type": "app",
  "url": "https://engrammar.pages.dev/",
  "description": "고등 영문법을 개념정리와 퀴즈로",
  "date": "2026-05",
  "icon": "📘",
  "status": "live"
}
```

- `district` — `museum · school · seonggyungwan · park · company · cafe · bank · house · me`
- `type` — `app`(새 탭) · `doc`(내부) · `video` · `external`
- `date` — 만든 시기 `YYYY-MM`. 목록의 정렬과 묶음이 이 값으로 그려진다 (§7). 빠지면 테스트가 잡는다
- `group` — 한 구역 안에서 소제목으로 묶을 때
- `status` — `live` · `beta` · `demo`
- `featured` — 상단 "추천 픽"에 노출
- `needsReview` — 설명이 아직 추정본이라 검수가 필요할 때만 단다. 지금은 전부 해제 상태

**설명은 만든 사람이 직접 쓴다.** 방문자가 각 작업물에 대해 읽는 유일한 문장이라
그럴듯하게 지어낸 문장을 두면 신뢰가 깎인다. 길이는 두세 문장까지 괜찮다 —
카드가 여러 줄이 되면 아이콘을 위에 맞춰 자연스럽게 받는다.

## 10. 남은 일

| 일 | 상태 |
|---|---|
| 마을 지도 · 구역 핫스팟 · "나" 캐릭터 · 팝오버 | ✅ |
| 목록 모달 (검색 · 타입 필터) · 추천 픽 | ✅ |
| 앰비언트 애니메이션 (자동차 · 새 · 구름 · 사람 · 건물) | ✅ |
| 아트북 뷰어 | ✅ |
| 뷰어를 여러 권 지원하도록 일반화 | ✅ |
| **세모집 카탈로그** | ✅ 올림 (2장 교체 완료, 나머지 문구는 §4 표) |
| **`/list` 정적 페이지 · sitemap · robots** | ✅ 크롤러가 보는 본문 74자 → 1,200자 |
| 회귀 테스트 (`npm test`, 121개) | ✅ |
| 편집용 원본을 배포에서 제외 (8.6MB → 3.4MB) | ✅ |
| `services.json` 설명 문구 검수 | ✅ 전부 직접 쓴 문장으로 교체 (2026-08-15 전면 재작성) |
| **목록을 만든 순서로 · 항목별 `date`** | ✅ 모달 · `/list` 양쪽 (§7) |
| **게임 안내서 `/game/`** (🔺 세모집) | ✅ 46면 · 챕터 1·2·3·4·42 (§6) · 판권면으로 출처·판본 정리 완료 |
| **방문 통계** (GA4 · 커스텀 이벤트 12종) | ✅ 켜짐 (`G-62X7QQW0GM`) · Cloudflare 비콘은 **토큰 대기** (§9 · [README](README.md)) |
| 카페 앞 까마귀 Croww (마스코트) | ✅ 세워 둠 · 문서가 없어 지금은 풍경 (§5) |
| **세모집 마당의 확성기 · 집 테마송** | ✅ 완료 — 노래(mp3)·가사 받음, 누르면 재생 + 가사 팝업 (§9 · [README](README.md)) |
| **날아다니는 쪽지 · 묶음 팝업** | ✅ 날고 눌림 · 받은 시트로 교체 완료 · 플래너(클리어 위크) 연결 |
| 위 묶음의 남은 자리 | 클리어 위크는 목록에 올랐다 · **캘린더 · 트래커 주소 대기** (만든 시기는 받음) |
| **Playgrown 전시 세션** (☕ 카페) | **열었다** (2026-08-21) · 넘기는 화면 아홉 · 카페 앞 까마귀가 문 |
| 위 전시의 재료 | **덱 3·4·8·10·11–16쪽 · 방문자 수 · 설문 결과 · 종료 판단 근거 대기** |
| 커스텀 도메인 (`dada.town`) | 선택 |
| 🌙 밤 마을 | 선택 |

## 11. 이름 · 도메인

- 이름: **DADA TOWN** (다다 마을)
- 현재 주소: `dada-portfolio.stupidpoohh.workers.dev`
- 커스텀 도메인(선택): `dada.town` — `*.workers.dev`는 링크 검사기에서 걸러지는 경우가 있어 공유용으로는 커스텀 도메인이 안전하다
- 히어로 카피: "만든 것들이 사는 마을, DADA TOWN."
