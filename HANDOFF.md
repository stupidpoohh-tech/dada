# 이어받기 (2026-08-22)

새 세션에서 이 저장소를 이어 작업할 때 **먼저 읽을 것.**

- **매 세션 자동으로 읽히는 것은 [CLAUDE.md](CLAUDE.md)다** — 브랜치 · 돌려보기 ·
  밟은 지뢰가 거기 있다. 여기서는 되풀이하지 않는다.
- 설계와 이유는 [README.md](README.md), 기획은 [PLAN.md](PLAN.md).
- **이 파일에는 「지금 어디까지 왔고 다음에 무엇을 하는가」만 적는다.**
  틀린 안내문은 없는 안내문보다 나쁘다 — 한 세션이 끝날 때마다 여기부터 고친다.

---

## 지금 상태

**두 브랜치 모두 `0359d90`.** 작업 트리 깨끗하고 `npm test` 189개 전부 통과.
손에 들고 있는 것 없음 — 아무 데서나 이어받아도 된다.

바로 앞 세션이 한 일: **PlayGrown 케이스 스터디를 처음부터 끝까지 지어 열었다.**
`/case/playgrown.html` · 화면 아홉(표지 + 절 여덟) · 마을 카페 앞 까마귀가 그 문.

---

## 다음에 할 일 — PlayGrown 마무리

### 1. 문서에서 빠진 것 넷 (제일 중요)

지어 놓고 보니 **「잘 만든 브랜드 룩북」이지 「케이스 스터디」가 아니다.**
읽을 수 있는 글자가 목차와 일력 18칸 빼면 500자 남짓이고, 그 안에 이것들이 없다.

| 빠진 것 | 왜 필요한가 | 누가 채우나 |
|---|---|---|
| **역할 한 줄** | 이 사이트는 링크드인을 대신한다. 「기획 · 브랜딩 · 공간 · 마케팅 · 현장 운영」 중 무엇을 했는지가 **어디에도 없다.** 보는 사람은 「예쁜 프로젝트다」까지만 가고 「이 사람이 뭘 하는 사람인지」는 못 얻는다 | 다원님 |
| **「파일럿 1회」가 첫 화면에 없다** | PLAN.md §5가 「그게 첫 화면에 있어야 뒤의 모든 문장이 제 무게로 읽힌다」고 못 박아 둔 것인데, 2026-08-22에 표지를 정리하면서 그 문장이 같이 지워졌다. **지금은 8번째 화면에서 갑자기 「파일럿」이 나온다** — 그때까지 읽는 사람은 계속 운영한 가게인 줄 안다. **의도가 아니라 사고다** | 바로 넣을 수 있음 |
| **숫자** | 몇 명 왔는지 · 설문 결과. 「사람이 실제로 왔다」는 최소한의 사실이 없어서 파일럿 절이 영상 카드 한 장이다. 문서의 클라이맥스여야 할 자리가 제일 얇다. **모르면 「세지 않았다」도 그대로 쓸 만하다** — 「행정가가 필요」라는 교훈과 바로 이어진다 | 다원님 |
| **사업성 판단의 근거 두세 문장** | 노트에 결론(교훈 3줄)만 있고 「무엇을 보고 그렇게 정했나」가 없다. 객단가 · 회전 · 인건비 · 운영 부담 중 무엇이었나. PLAN.md §5가 **「이 문서에서 가장 값나가는 두세 문장」**이라고 꼽은 자리 | 다원님 |

**노트 절(9번째 화면)은 다원님이 준 세 줄이 전부다.** 대신 쓴 회고 문장이 한 줄이라도
섞이면 그 절 전체가 남의 말이 된다 — 2026-08-22에 그렇게 정했다. 채울 때도 같은 규칙.

### 2. 폰에서 눈으로 봐야 할 것 하나

**두 손가락으로 벌렸다 오므렸을 때 화면이 제자리로 오는지.** 컨테이너에서는 손가락을
흉내 낼 수 없어 코드로만 맞춰 뒀다(`playgrown.js`의 `visualViewport` 부분).
같은 자리에서 다중 터치가 쓸기로 오인되던 것도 고쳤으니 **벌렸다 놓아도 화면이
안 넘어가야** 한다.

### 3. 아직 안 정한 것 하나

**루트 원본을 GitHub 쪽에서도 뺄지.** 배포에서는 `.assetsignore`로 뺐지만
(`/[0-9].*`), 저장소가 공개라면 GitHub으로는 여전히 받아진다. 칼럼 원본
(`1.column.png`)에는 **인터뷰이의 실명과 나이**가 남아 있고 배포본은 그 부분을
아예 잘라 냈다. 지우려면 이력까지 다시 써야 한다 — 다원님이 정한다.

---

## PlayGrown 지도 — 어디에 무엇이 있나

```
case/playgrown.html            화면 아홉이 순서대로. 절마다 왜 그 꼴인지 주석에 있다
assets/case/playgrown.css      이 페이지 전용. 마을 것(styles.css)과 섞지 않는다
assets/case/playgrown.js       둘뿐이다 — 영상 바꿔 끼우기, 화면 넘기기
assets/case/playgrown/*.jpg    보드 셋 + 조감도 (770KB)
tools/cut_case.mjs             그림 자르기. 원본은 _material/ (gitignore)
tests/smoke.mjs                「케이스 스터디」로 시작하는 절들이 이 페이지를 지킨다
```

**손대기 전에 알아 둘 것 넷** (자세한 것은 CLAUDE.md 지뢰밭과 코드 주석에):

- **`.viewer`를 붙이는 것은 JS다.** 없으면 그냥 굴리는 문서로 남는다.
  이 되돌림을 깨지 않는다 — 회귀 테스트가 지킨다
- **굴릴 수 있는 곳은 하나뿐이어야 한다.** 문서는 100dvh로 못 박고 굴리는 것은
  화면 안에서만
- **일력 규칙은 두 벌이다** — px 한 벌, `@supports (container-type)` 안에 cqw 한 벌.
  cqw만 적으면 그것을 모르는 브라우저에서 형체가 무너진다
- **playwright의 locator로 누르거나 굴리지 않는다** — 스크롤 구동 애니메이션 때문에
  「멈출 때까지」가 영영 안 온다. `evaluate` 안에서 `element.click()`

---

## 이 문서 말고 함께 갱신할 것

절이 다 찼으므로 **CLAUDE.md의 「지금 짓고 있는 것」 절은 이미 걷었다.**
PlayGrown이 완전히 끝나면 여기 「다음에 할 일」도 지우고 다음 일을 적는다.
PLAN.md §5에 결정 이력이 날짜별로 쌓여 있다 — 왜 그렇게 됐는지는 거기서 읽는다.
