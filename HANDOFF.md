# 이어받기 (2026-08-23)

새 세션에서 이 저장소를 이어 작업할 때 **먼저 읽을 것.**

- **매 세션 자동으로 읽히는 것은 [CLAUDE.md](CLAUDE.md)다** — 브랜치 · 돌려보기 ·
  밟은 지뢰가 거기 있다. 여기서는 되풀이하지 않는다.
- 설계와 이유는 [README.md](README.md), 기획은 [PLAN.md](PLAN.md).
- **이 파일에는 「지금 어디까지 왔고 다음에 무엇을 하는가」만 적는다.**
  틀린 안내문은 없는 안내문보다 나쁘다 — 한 세션이 끝날 때마다 여기부터 고친다.

---

## 지금 상태

**두 브랜치 모두 같은 자리.** 작업 트리 깨끗하고 `npm test` 230개 전부 통과.
손에 들고 있는 것 없음 — 아무 데서나 이어받아도 된다.

**주소가 `dada-town.com`이 됐다** (2026-08-24). Cloudflare Registrar에서 산
도메인이라 대시보드에서 Custom Domain으로 바로 붙였다. 저장소 쪽에서 함께 간 것은
넷 — `services.json`의 `site.url`, 세 페이지의 `canonical`·`og:*`, `ga.js`의
`HOSTS`, `wrangler.jsonc` 주석. 표는 README 「주소 — dada-town.com」에 있다.
**`www.`는 아직 못 붙였다** (아래 「다음에 할 일」).

**게임 안내서(`/game/`)의 잘림을 고쳤다** (2026-08-23). 넓은 화면과 가로로 돌린
폰에서 슬라이드 아래가 잘려 나가고 있었다 — 개발용 1440px에서는 멀쩡해 보여
오래 안 보였다. 이유는 PLAN.md §6 「한 면이 통째로 안 들어가고 있었다」에,
같은 실수를 다시 안 하려고 CLAUDE.md 지뢰밭에도 한 줄 넣었다.

**PlayGrown은 끝났다.** 바로 앞 세션이 문서에서 빠져 있던 넷을 정리하고
루트 원본을 걷어 냈다. 결정과 이유는 PLAN.md §5 「마무리 — 문서가 사람을
말하게 한다 (2026-08-23)」에 있다. 요지만 적으면:

- 표지에 **「파일럿 1회」**를 되돌렸다 (2026-08-22에 사고로 지워졌던 것)
- 표지에 **역할 한 줄** — 기획 · 브랜딩 · 공간 · 마케팅 · 현장 운영 전부
- 파일럿 절 끝에 **접은 이유** — 「체험 동선 설계를 다시 손봐야 한다」
- **숫자(방문자 수 · 설문)는 안 넣기로 했다** (다원님). 다음 세션에서 이것을
  「빠진 것」으로 다시 집어넣지 않는다 — **정해진 것이다**
- 루트 원본 열둘(`[0-9].*`, 14.6MB)을 **트리에서 지웠다.** 이력은 그대로 둔다
- 다원님이 폰으로 넘겨 보고 짚어 준 대로 **절 이름 · 리드 · 바탕색을 다시 잡았다.**
  일력과 마케팅 밑의 해설 문단은 걷었다 — 그림이 이미 하는 말이었다.
  **여기서부터 절 이름은 다원님 말이다.** 되돌리거나 다듬지 않는다

- **다원님이 폰에서 짚어 준 넷을 고쳤다** (2026-08-24) — 학교 팝오버 차례,
  케이스 스터디 첫 화면에서 왼쪽으로 나가기, 회사 건물 자리, 「준비 중」인 것을
  전체 목록에 올리기. 자세한 것은 PLAN.md §5 「다원님이 폰에서 짚어 준 넷」.
  **`status: "soon"`인 항목은 링크가 되면 안 된다** — 세 군데(목록 · 모달 ·
  구역 패널)가 각자 다른 코드라 하나만 고치면 나머지가 샌다
- **브랜드 보드를 고친 판(`4.brand-board3.png`)으로 갈았다** (2026-08-24).
  말풍선 넷이 틀려 있었다 — 선배텡→스팀게임, 친쿡취→3D펜, 계컹→캐릭.
  `alt`도 그 대목만 같이 고쳤다. 루트 원본 둘은 트리에서 걷었다

앞의 셋은 회귀 테스트가 지킨다 — `node tests/smoke.mjs 표지가`.

---

## 다음에 할 일

**`www.dada-town.com`을 붙이는 것이 남았다.** apex(`dada-town.com`)는 Active인데
www를 Custom Domain으로 넣으면 「No zones match」가 났다 (2026-08-24).
`ga.js`의 `HOSTS`에는 미리 적어 뒀으므로 **붙는 순간 통계는 저절로 켜진다** —
저장소 쪽에서 더 할 일은 없다. 안 붙어도 사이트는 멀쩡하다.

그 밖에는 **정해진 것 없음.** 다음 일은 다원님이 정한다. 고를 거리가 필요하면
PLAN.md §10 「남은 일」을 본다.

**두 손가락 확대는 2026-08-23에 다원님이 폰에서 확인했다** — 벌렸다 오므리면
제자리로 오고, 놓아도 화면이 안 넘어간다. 컨테이너에서는 손가락을 흉내 낼 수
없어 코드로만 맞춰 뒀던 것(`playgrown.js`의 `visualViewport`)이라 확인이
필요했는데, 이제 끝났다. 그 근처를 손댈 일이 생기면 **다시 폰에서 봐야 한다** —
검사로는 못 잡는다.

---

## PlayGrown 지도 — 어디에 무엇이 있나

```
case/playgrown.html            화면 아홉이 순서대로. 절마다 왜 그 꼴인지 주석에 있다
assets/case/playgrown.css      이 페이지 전용. 마을 것(styles.css)과 섞지 않는다
assets/case/playgrown.js       둘뿐이다 — 영상 바꿔 끼우기, 화면 넘기기
assets/case/playgrown/*.jpg    보드 셋 + 조감도 (770KB)
tools/cut_case.mjs             그림 자르기. 원본은 _material/ (gitignore)
tests/smoke.mjs                「케이스 스터디」로 시작하는 절들이 이 페이지를 지킨다
```

**손대기 전에 알아 둘 것 다섯** (자세한 것은 CLAUDE.md 지뢰밭과 코드 주석에):

- **`.viewer`를 붙이는 것은 JS다.** 없으면 그냥 굴리는 문서로 남는다.
  이 되돌림을 깨지 않는다 — 회귀 테스트가 지킨다
- **굴릴 수 있는 곳은 하나뿐이어야 한다.** 문서는 100dvh로 못 박고 굴리는 것은
  화면 안에서만
- **일력 규칙은 두 벌이다** — px 한 벌, `@supports (container-type)` 안에 cqw 한 벌.
  cqw만 적으면 그것을 모르는 브라우저에서 형체가 무너진다
- **playwright의 locator로 누르거나 굴리지 않는다** — 스크롤 구동 애니메이션 때문에
  「멈출 때까지」가 영영 안 온다. `evaluate` 안에서 `element.click()`
- **노트 절(마지막 화면)은 다원님이 준 세 줄이 전부다.** 대신 쓴 회고 문장이
  한 줄이라도 섞이면 그 절 전체가 남의 말이 된다 — 2026-08-22에 그렇게 정했다.
  파일럿 절의 판단 한 문단도 같은 규칙으로 썼다: **받은 한 줄에 살을 안 붙인다**

**`.assetsignore`의 `/[0-9].*`는 지우지 않는다.** 지금 걸릴 파일은 없지만,
다원님이 그림을 올리는 자리가 저장소 루트라 다음에 올라오는 원본이 그것 없이는
곧장 배포된다. 규칙이 놀고 있는 것처럼 보여도 그대로 둔다.

---

## 이 문서 말고 함께 갱신할 것

PLAN.md §5에 결정 이력이 날짜별로 쌓여 있다 — 왜 그렇게 됐는지는 거기서 읽는다.
다음 일이 정해지면 위의 「다음에 할 일」을 그것으로 바꾼다.
